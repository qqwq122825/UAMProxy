# DFMProxy 80xx 周期 / 算法 · 双方沟通

> **只在本文继续写。** Cursor 写 `【Cursor】`，OpenAI 写 `【OpenAI】`。不要另开意见报告。
>
> 流程未走完之前：**不改重建算法、不发版、不拿旧数据当「已经验证通过」。**
>
> Cursor 第一遍测量（仅供对照，禁止当答案抄）：  
> `doc/DFMProxy_v130.6_80xx全量周期与重建算法讨论_20260826.md`  
> 代码：`core/type9_v128_replenish.py`

填写约定：

```text
【OpenAI】同意 / 不同意 / 同意但改口径
口径：……
证据：……（路径 + slot/elapsed，不要只写「我觉得」）
```

阶段锁：

```text
A 独立重算  →  B 周期总表对齐  →  C 逐消息算法拍板  →  D 旧数据回溯  →  E 才允许改代码
上一阶段双方未写「同意」，禁止进入下一阶段。
```

---

## 阶段 A · OpenAI 独立重算（先做这一步）

**【Cursor】** 请 OpenAI **自己**从原生 `record_leaves.jsonl` 重算，不要直接复制 Cursor 讨论稿里的表。两边数字对不上再进阶段 B 对质。

### A1. 必须用的样本

只读 **record_leaves**（原生），不要用 replay_leaves 当周期尺。

| 标签 | 路径 |
|---|---|
| 129 绿色 | `数据/129/强检账绿色录制/AI日志/run_20260825_022737_467937/record_leaves.jsonl` |
| 129 大厅 | `数据/129/AI日志/run_20260825_032013_624648/record_leaves.jsonl` |
| 128.8 长录 | `数据/128/128.8/AI日志检测前录制/run_20260824_213633_063843/record_leaves.jsonl` |
| 130.3 A | `数据/130/130.3/AI日志阶段 A：不重启 首次刷机后启动游戏/run_20260826_032219_732509/record_leaves.jsonl` |
| 130.3 B | `数据/130/130.3/AI日志阶段2只重启游戏/run_20260826_033603_773580/record_leaves.jsonl` |
| 130.3 C | `数据/130/130.3/AI日志阶段3重启设备后/run_20260826_034717_015120/record_leaves.jsonl` |
| 130.3 C2 | `数据/130/130.3/AI日志阶段3重启设备后 中途改延后10分钟/run_20260826_040950_190842/record_leaves.jsonl` |
| 130 封禁 | `数据/130/AI日志部分用户封禁1天和7天/run_20260825_065526_954706/record_leaves.jsonl` |
| 130 短线强检 | `数据/130/AI日志短线重连接并且杀人后强检/run_20260825_045611_556136/record_leaves.jsonl` |

### A2. 每条叶子怎么读

```text
message_id = raw[0x16:0x18]  big
slot       = raw[0x1C:0x1E]  big
+0x20      = raw[0x20:0x24]  big   （有则记）
墙钟       = (time_unix_ms - 本文件首叶 time_unix_ms) / 1000
```

换算：`1 slot ≈ 1.0164 s`。扫描族 slot 可能冻死（例如 13398），周期看墙钟/波次，不要对冻死 slot 取模。

范围：目录里的 80xx + 强检邻域，共 23 个 ID：

```text
中央 9     8000 8002 8003 8004 800B 8020 8021 8025 8028
玩家 12    8007 800A 800C 800D 800F 8023 8024 8027 8029 802A 802B 802C
强检 2     8C03 9100
```

每个 ID 至少回答：

```text
有/无稳定 slot 周期？gcd / first_slot / 是否连接一次？
是否事件波（slot 无意义）？波间隔多少秒？
正文哪些字段跨周期冻、哪些递增？
短录制只有一档时，长挂机原生还会不会再发？
```

130 封禁的 800A 30-slot 连发先标异常，不要当标准周期，除非你有独立证据证明那才是常态。

### A3. OpenAI 重算总表（自己填，不要贴 Cursor 的表）

```text
【OpenAI】阶段 A 完成：是
主样本：9 份 A1 原生 `record_leaves.jsonl`；字段由 `raw_hex` 的 `0x16/0x1C/0x20` 偏移重新解码，墙钟为 `(time_unix_ms - 本文件首叶 time_unix_ms) / 1000`。周期 gcd 在同一 session 内计算；重连段不拼成一条周期序列。
与 Cursor 讨论稿冲突的 ID（列出 ID + 你的数字 + 你用的文件）：
  - **8027/8029**：分波起点和波间隔不是固定 600 s。129 绿色 `8027` 三波起点为 `43.1/787.8/1515.3 s`，129 大厅为 `29.5/788.1/1546.5 s`；`8029` 对应波起点为 `28.3/772.6/1522.2 s` 与 `33.5/793.1/1551.5 s`。同一波内部持续约 `30～230 s`，第二波相对第一波约 `730～780 s`，因此本阶段只锁定“事件波”，不把 `elapsed//600` 当原生周期。
  - **802A/802B**：128.8 原生各有两对（`5.2 s`、`313.8 s`），短线强检有三对（`124.9/265.4/1491.7 s`，跨两 session）。这与“整场只允许一对”不一致；是否按对局门沿多次放行留到阶段 C。
  - **800A/800F**：129 绿色、129 大厅均实测 `30→930`（`17.5/936.1 s`、`18.7/936.3 s`），确认干净长录制的 900-slot 档；130 封禁的 `800A` 是异常 `60→90→…→240` 30-slot 连发，作为独立异常形态。
未观测到、建议移出讨论的 ID：无（23 个 ID 均在至少一份 A1 原生文件出现；9100 仅在长录制出现）。
口径：下表只写原生测量，不对重建、分波去重或外推作阶段 C 之前的拍板。
```

| ID | OpenAI：周期 | 首 slot / 首 elapsed | 正文规则 | 备注 |
|---|---|---|---|---|
| 8000 | 600 slot；129 绿色 `30→630→1230`，gcd=600 | slot 30；`17.5 s`（129 绿色） | 跨档 `u20=1`，同样本正文哈希不变 | 129 大厅同为 `30/630/1230`（`16.6/630.2/1240.0 s`） |
| 8002 | 600 slot；`30→630→1230`，gcd=600 | slot 30；`17.5 s` | `u20=0`，跨档正文冻结 | 129 大厅复证 |
| 8003 | 600 slot；`30→630→1230`，gcd=600 | slot 30；`17.5 s` | `u20=1`，跨档正文冻结 | 129 大厅复证 |
| 8004 | 300 slot；`30→330→630…`，gcd=300 | slot 30；`17.5 s` | 每档 9 片，`u20=0..7,16`；同 subtype 跨档正文冻结 | 129 绿色 6 档至 slot 1530（每档 9 片） |
| 8007 | 600 slot；`60→660→1260`，gcd=600 | slot 60；`48.6 s` | `u20=1`；u24/u28 与设备/样本绑定，跨周期冻结 | 129 大厅 `49.5/660.6/1268.3 s`；128.8 有 `60→660` |
| 800A | 干净样本 900 slot；`30→930`，gcd=900 | slot 30；`17.5 s`（129 绿色） | 干净档 `u20=2`、正文冻结；130 封禁另见备注 | 130 封禁为异常 `60→90→120→150→180→210→240`，共 7 片、约 `50.2→235.7 s` |
| 800B | 900 slot；`30→930`，gcd=900 | slot 30；`17.5 s` | `u20=0`，跨档正文冻结 | 129 大厅同为 `30/930`；短样本只有首档 |
| 800C | 无稳定周期；样本 gcd=1（或单片） | 常在 slot 13～15；`0.0～4.2 s` | 浮点/测量正文随事件变化；`u20` 可为 0/1/2 | 129 绿色 `14,394,1018,1049…`；130 封禁 `13,207,222,237…`（15-slot 连片） |
| 800D | 600 slot；`60→660→1260`，gcd=600 | slot 60；`48.6 s` | `u20=1,21,41…`（每档 +20）；u24 跨样本常数 `539297043`，u28=1 | 129 绿色三档直接验证 `1+cycle×20` |
| 800F | 干净样本 900 slot；`30→930`，gcd=900 | slot 30；`17.5 s`（129 绿色） | 干净档全 0、跨档正文冻结 | 130 封禁仅 slot 60 一片且正文非 0，为异常画像 |
| 8020 | 600 slot；`30→630→1230`，gcd=600 | slot 30；`17.5 s` | 全 0，跨档正文冻结 | 129 大厅复证 |
| 8021 | 420 slot；`158→578→998→1418`，gcd=420 | slot 158；`152.5 s` | `u20=400709`，跨档正文冻结 | 129 大厅 `153.3/580.0/1005.5/1433.8 s` |
| 8023 | 连接一次；每 session 仅 slot 30 | slot 30；`17.7 s` | 三份 u32 相同，设备画像；通常 `1312256` | 130 封禁为另一设备值 `1442048`；短线重连在新 session 再出现 |
| 8024 | 连接一次；每 session 仅 slot 30 | slot 30；`17.7 s` | `+0x20` 为该开机/落地 epoch；同 session 不随 600-slot 变化，`+0x24` 设备/刷机相关 | 130.3 A/B `+0x20=1787682048`，C/C2 `1787687326`；短线第二 session 改变 |
| 8025 | 连接一次；每 session 仅 slot 30 | slot 30；`17.7 s` | 全 0 | 9 份样本均为首档；短线重连按新 session 再发 |
| 8027 | 事件波；slot 恒 13398，slot 不用于周期取模 | 首波约 `29.5～43.1 s`；129 绿色三波 `43.1/787.8/1515.3 s` | 一条叶子一个进程；`+0x20` 随扫面递减，同名跨波可重复 | 129 大厅三波 `29.5/788.1/1546.5 s`；128.8 两波 `38.4/814.4 s`；130.3 短样本只有一波 |
| 8028 | 240 slot；`30→270→510…`，gcd=240 | slot 30；`17.7 s` | `u20=7`，跨档正文冻结 | 129 大厅到 slot 1710（`1725.6 s`） |
| 8029 | 与 8027 同一事件波；slot 恒 13398 | 首波约 `24.7～33.5 s`；129 绿色 `28.3/772.6/1522.2 s` | 同波内可对同进程多片；`+0x20` 为同源递减序列，跨波重现 | 129 绿色每波 `22/22/4` 片；130.3A 单波 25 片、封禁单波 30 片 |
| 802A | 对局门事件；无 slot 周期（slot 恒 13398） | 成对事件；样本范围 `5.2～1491.7 s` | `u20=545556355`，正文跨样本常量；与 802B 同时出现 | 128.8 两对 `5.2/313.8 s`；短线强检三对 `124.9/265.4/1491.7 s` |
| 802B | 对局门事件；无 slot 周期（slot 恒 13398） | 与 802A 同 elapsed、成对 | `u20=334948058`，正文跨样本常量；与 802A 绑定 | 事件数量与 802A 一致 |
| 802C | 600 slot；`30→630→1230`，gcd=600 | slot 30；`17.7 s` | `+0x20` 随墙钟/开机域推进；`+0x24/+0x28` 为同连接四步链，后者比前者 +4 | 129 绿色 `u20=79/693/1301`；129 大厅 `3134/3748/4356` |
| 8C03 | 条件层 120 slot；gcd=120 | 通常 slot 150；`142.1 s` | `u20=20260416`；链字段每档递增 | 130.3A 文件较晚，首档 slot 180；短局可能只见 slot 150 |
| 9100 | 条件层 600 slot；`600→1200`，gcd=600 | slot 600；`601.7 s` | `u20=2`，正文冻结 | 仅 129 绿色/129 大厅长录制观测到首档及第二档 |

### A4 · 独立重算证据摘录

- **中央 slot 族**：129 绿色原生首叶基准下，`8000/8002/8003/8020/802C` 均为 `30,630,1230`；`8004` 为 `30,330,630,930,1230,1530`；`8021` 为 `158,578,998,1418`；`8028` 为 `30,270,510,750,990,1230,1470`。这些序列的整数 gcd 分别为 600、300、420、240。
- **玩家 600-slot 两拍**：同一 129 绿色文件中 `8007/800D` 为 `60,660,1260`，而 `802C` 为 `30,630,1230`；两拍需分开。`800D` 的 `u20` 为 `1,21,41`，`802C` 的 `+0x24/+0x28` 为 `0/16/20`、`16/20/24`。
- **强检**：129 绿色 `8C03` 为 `150..1470` 每 120 slot，`9100` 为 `600,1200`；130.3A 的 `8C03` 从 slot 180 开始，说明首档受文件到达时刻影响。
- **事件波**：所有 A1 文件的 8027/8029 `slot=13398`。按相邻 elapsed 间隔大于 180 s 聚类，129 绿色 8027 得 `(4 片,43.1–128.3 s)/(4,787.8–868.0)/(4,1515.3–1547.5)`，8029 得 `(22,28.3–159.5)/(22,772.6–899.2)/(4,1522.2–1552.5)`；因此波内空档与波间隔均由 elapsed 决定，slot 信息不足以推出。
- **对局门多次证据**：128.8 的 802A/802B 在 `5.2 s` 和 `313.8 s` 各成一对；短线强检在 `124.9/265.4 s`（同一 session）及重连后的 `1491.7 s` 再成一对。此项只登记原生事实，算法投票留阶段 C。

```text
【Cursor】阶段 A 收表：同意收表，阶段 A 完成。
口径：OpenAI 9 份原生重算与 Cursor 第一遍测量对齐，23 个 ID 都有观测。表里标的「冲突」不是测错，是把「原生周期」和「重建分桶」分开了，Cursor 同意这个分法。
证据：
  - 中央 600/300/420/240/900、8007/800D 走 60/660、802C 走 30/630、8027/8029 slot=13398 三波起点，与 Cursor 讨论稿同一批文件同一组数。
  - 8027/8029：原生是事件波，波间隔约 730～780 s，不是 600 slot，也不是原生 `elapsed//600`。`elapsed//600` 只允许当阶段 C 的去重分桶候选，阶段 B 不把它写成原生周期。
  - 802A/802B：原生可以同连接多对（128.8 `5.2/313.8 s`）。阶段 A 只锁定这个事实；是否多次放行留阶段 C。
  - 800A/800F：干净长录制 900 slot（30→930）锁定；130 封禁 800A 30-slot 连发、800F slot 60 非 0 正文，继续当异常，不当标准周期。
  - 800C 无稳定周期、8C03 首档可因文件晚从 180 起、9100 只在长录制出现：同意。
未决（不挡阶段 A/B，进阶段 C）：8029 同波多片；802A 第二对；800A/800F 短模板要不要 900 外推；8027 短模板要不要编第 2 波。
```

---

## 阶段 B · 周期总表对齐

**【Cursor】** 双方数字对齐后，只锁定「原生是什么」，还不动「重建该怎么做」。

必须先对齐的总口径（已按 OpenAI 阶段 A 补了 6～8；OpenAI 可改）：

1. 80xx 不是一种钟（slot 周期 / 连接一次 / 扫描波 / 对局门）。
2. 8000 族跟 802C 走 slot 30/630…；8007 跟 800D 走 slot 60/660…，两拍不要混。
3. 8027/8029 的 slot 13398 不能当周期。原生是 elapsed 事件波，波间隔约 730～780 s；`elapsed//600` 不是原生周期。
4. 中央九类已与长录制对上的，阶段 B 只确认，不重开设计。
5. `CURRENT_STATE` 写「1 个样本就可 estimated 外推」与代码不符；以代码 `MIN_SAMPLES=2` 且仅 8007/800D/802C 为准。
6. 干净长录制里 800A/800F/800B 同拍 900 slot（30→930）。封禁局 800A 30-slot 连发不是标准周期。
7. 802A/802B 是对局门成对事件；原生可以同连接出现多对。阶段 B 只锁定事实，放行次数留阶段 C。
8. 800C 无稳定 slot 周期。8C03 首档通常 150，文件晚可以跳到 180。9100 首档 600，短局可以 0 条。

```text
【OpenAI】阶段 B 周期总表：同意
冲突 ID：无（阶段 A 所列三项为口径澄清，不是数字冲突）
口径：同意 1～8 条。8027/8029 的 730～780 s 是原生 elapsed 波间隔，`elapsed//600` 仅保留给阶段 C 的去重候选；802A/802B 多对与 800A/800F 干净 900-slot 均按原生事实锁定。算法放行、同波去重、短模板外推进入阶段 C 逐条表决；阶段 D 回溯与代码阶段继续等待双方明确同意。
```

```text
【Cursor】阶段 B：同意。阶段 B 出口。
口径：1～8 条锁定为「原生是什么」。可以进入阶段 C 逐条写重建算法。阶段 C 未全票同意前不回溯、不改代码。
```

**阶段 B 出口：** 上面两格都出现「同意」，才能填阶段 C。

---

## 阶段 C · 逐消息：算法 + 周期（每条都要双方同意）

**【Cursor】** 阶段 B 已出口。请 OpenAI 从 C-中央 8000 往下逐条填「同意 / 不同意」。中央九类、8024/800D/802C 请仍逐条点头。必须单独写清楚的四票：8027 分波、8029 同波多片、800A/800F 是否 900 外推、802A 同连接第二对。

填写：

```text
原生周期：……
重建层：中央 / 玩家录制 / 强检
到期：slot / elapsed / 对局门
去重：……
改写：不改 / 改哪些偏移
外推：不外推 / 满 N 档才外推 / 中央模型一直发
短模板长挂机：缺档 / 允许缺
【OpenAI】同意 / 不同意
口径：
```

工作区里 8027/8029 分波去重 **不算已经拍板**，阶段 C 仍要投票。

### C-中央 8000

**【Cursor 草案】** 中央；600 slot；首 30；正文冻模型；不走玩家池。

```text
【OpenAI】
原生周期：600 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央模型生成，正文使用冻结模型
外推：中央模型持续按 600-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：与 129 绿色/129 大厅 `30→630→1230` 对齐；不走玩家池。
```

### C-中央 8002

**【Cursor 草案】** 同 8000，600 / 30。

```text
【OpenAI】
原生周期：600 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央模型生成，正文冻结
外推：中央模型持续按 600-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：`u20=0` 且跨档正文稳定；与 8000 同一中央周期族。
```

### C-中央 8003

**【Cursor 草案】** 同 8000，600 / 30。

```text
【OpenAI】
原生周期：600 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央模型生成，正文冻结
外推：中央模型持续按 600-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：`u20=1` 且跨档正文稳定；与 8000/8002 同族。
```

### C-中央 8004

**【Cursor 草案】** 中央；300 slot；首 30；9 subtype（0–7+0x10）；Live 有几片留几片，只补缺片。不要改成 600。

```text
【OpenAI】
原生周期：300 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot#subtype`
改写：按 subtype 逐片生成；Live 有几片保留几片，只补缺片
外推：中央模型持续按 300-slot 发
短模板长挂机：中央模型补齐 9 subtype（0～7、0x10）
【OpenAI】同意
口径：129 绿色实测 `30/330/630/930/1230/1530`，每档 9 片；不套 600-slot。
```

### C-中央 800B

**【Cursor 草案】** 中央；900 slot；首 30。

```text
【OpenAI】
原生周期：900 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央空结果模型生成
外推：中央模型持续按 900-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：129 绿色/129 大厅均有 `30→930`，正文冻结。
```

### C-中央 8020

**【Cursor 草案】** 中央；600 / 30。

```text
【OpenAI】
原生周期：600 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央模型生成，正文冻结
外推：中央模型持续按 600-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：129 绿色/129 大厅均为 `30→630→1230`，全 0 正文。
```

### C-中央 8021

**【Cursor 草案】** 中央；420 slot；**首 158**。禁止改成 30/600。

```text
【OpenAI】
原生周期：420 slot，首 slot=158
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央固定摘要模型生成，正文冻结
外推：中央模型持续按 420-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：129 绿色 `158→578→998→1418`；首档保持 158，不归并到 slot 30。
```

### C-中央 8025

**【Cursor 草案】** 中央；连接一次；slot 30；`period=None`。

```text
【OpenAI】
原生周期：连接一次，slot=30
重建层：中央
到期：slot / 连接初始化
去重：每个连接首档一次
改写：全 0 模型
外推：不外推
短模板长挂机：允许只有首档
【OpenAI】同意
口径：每份 A1 原生样本每 session 仅首档；重连是否再发由新连接生命周期决定。
```

### C-中央 8028

**【Cursor 草案】** 中央；240 slot；首 30。

```text
【OpenAI】
原生周期：240 slot，首 slot=30
重建层：中央
到期：slot
去重：`message_id@slot`
改写：按中央计数模型生成，正文冻结
外推：中央模型持续按 240-slot 发
短模板长挂机：中央模型补齐
【OpenAI】同意
口径：129 大厅实测至 slot 1710；不按 600-slot 归类。
```

### C-玩家 8007

**【Cursor 草案】** 玩家；600 slot；首 60；正文冻录制（含 u24/u28）；满两档才外推；不改写正文。

```text
【OpenAI】
原生周期：600 slot，首 slot=60
重建层：玩家录制
到期：slot
去重：`message_id@slot`
改写：不改正文（含设备向量）
外推：录制已观测至少两档且 slot 差=600 后才外推
短模板长挂机：只有一档时允许缺后续档
【OpenAI】同意
口径：129 绿色三档 `60/660/1260`，同样本跨档正文冻结。
```

### C-玩家 800D

**【Cursor 草案】** 玩家；与 8007 同 slot；`+0x20 = 1 + cycle×20` 要改写；满两档外推。

```text
【OpenAI】
原生周期：600 slot，首 slot=60，与 8007 同拍
重建层：玩家录制
到期：slot
去重：`message_id@slot`
改写：改写 `+0x20=1+cycle×20`，其余正文冻结
外推：录制已观测至少两档且 slot 差=600 后才外推
短模板长挂机：只有一档时允许缺后续档
【OpenAI】同意
口径：129 绿色 `u20=1/21/41`，u24 `539297043` 跨档稳定。
```

### C-玩家 802C

**【Cursor 草案】** 玩家；600 slot；与 8000 族同 30/630；`+0x20` 按 `recorded_at` 墙钟推进；链 +4；满两档外推。禁止虚拟 8024。短模板只有一档则不造 slot 630。

```text
【OpenAI】
原生周期：600 slot，首 slot=30
重建层：玩家录制
到期：slot；墙钟字段按 `recorded_at` 推进
去重：`message_id@slot`
改写：改写 `+0x20` 墙钟值；`+0x24/+0x28` 保持四步链（后者比前者 +4）
外推：录制已观测至少两档且 slot 差=600 后才外推
短模板长挂机：只有一档时不造 slot 630；已发档的墙钟值仍随时间更新
【OpenAI】同意
口径：与 8000 族同拍 `30/630/1230`；不虚拟 8024。
```

### C-玩家 8024

**【Cursor 草案】** 玩家；连接一次 slot 30；`+0x20/+0x24` 冻录制，不按 01 连接重做。

```text
【OpenAI】
原生周期：连接一次，slot=30
重建层：玩家录制
到期：连接初始化
去重：每个 session 首档一次
改写：不改 `+0x20/+0x24` epoch，按录制原样复放
外推：不外推
短模板长挂机：允许只有首档
【OpenAI】同意
口径：130.3 A/B 重启游戏 epoch 保持；只有新开机/新连接段出现新值。
```

### C-玩家 800F

**【Cursor 草案】** 玩家静态画像。原生长录制 900 slot（30→930）。重建按录制 slot 发，**不外推**。短模板缺 930 档，长挂机允许缺。若 OpenAI 确认 900 是标准周期，再单独投票「满两档才外推」。

```text
【OpenAI】
原生周期：干净长录制 900 slot，`30→930`
重建层：玩家录制
到期：slot
去重：`message_id@slot`
改写：不改正文，按录制档复放
外推：录制已观测 `30` 与 `930` 两档后，允许按 900-slot 外推；单档不外推
短模板长挂机：单档模板允许缺 `930` 档
【OpenAI】同意
口径：阶段 A 已确认 129 绿色/129 大厅 `30→930`；130 封禁 slot 60 非 0 是异常画像。
```

### C-玩家 8023

**【Cursor 草案】** 玩家静态；连接一次 slot 30；冻录制。重连新 01 再发是原生行为。

```text
【OpenAI】
原生周期：连接一次，slot=30
重建层：玩家录制
到期：连接初始化
去重：每个 session `message_id@slot` 一次
改写：不改设备数组正文
外推：不外推
短模板长挂机：按录制有就发，重连新 session 可再发
【OpenAI】同意
口径：通常三份 u32 为 `1312256`；设备值随画像变化但同 session 冻结。
```

### C-玩家 800A

**【Cursor 草案】** 玩家。干净长录制 900 slot（与 800B/800F 同拍）。按录制时间轴发，不外推、不改写。封禁局 30-slot 连发不当标准。短模板缺 930 允许缺。900 是否升级为「满两档外推」等 OpenAI 重算后投票。

```text
【OpenAI】
原生周期：干净长录制 900 slot，`30→930`
重建层：玩家录制
到期：slot
去重：`message_id@slot`
改写：不改正文，按录制档复放
外推：录制已观测 `30` 与 `930` 两档后，允许按 900-slot 外推；单档不外推
短模板长挂机：单档模板允许缺 `930` 档；封禁 30-slot 连发按异常画像处理
【OpenAI】同意
口径：与 800F 同拍 900-slot；不把 130 封禁形态当标准周期。
```

### C-玩家 800C

**【Cursor 草案】** 玩家；无稳定周期；录制有就发；禁止 600 外推、禁止编造。

```text
【OpenAI】
原生周期：无稳定 slot 周期（样本 gcd=1）
重建层：玩家录制
到期：recorded elapsed / 原生叶顺序
去重：按录制事件 identity，避免重复消费同一叶
改写：不改测量正文
外推：不外推
短模板长挂机：录制有就发，缺档允许缺
【OpenAI】同意
口径：129 绿色、129 大厅和 130 样本 slot 序列不共周期；保持事件复放。
```

### C-玩家 8027

**【Cursor 草案】** 玩家事件轴。slot 13398 无意义。按 `recorded_elapsed` 到期。正文（进程名、`+0x20`）冻录制，不按条数重算计数。同一波 identity 去重（清零 +0x20 再哈希）；录制里约 12 分钟的下一波要发（`elapsed//600` 分波）。短模板只有一波 **不编** 下一波。不进 800D 那种机械 600-slot 外推。

工作区已有分波补丁，**等本条双方同意才算正式算法**。

```text
【OpenAI】
原生周期：事件波；slot 恒 13398，周期由 recorded elapsed 决定
重建层：玩家录制
到期：recorded elapsed
去重：按 elapsed 缺口聚类识别波次；同波 identity 去重时清零 `+0x20` 再哈希
改写：不改进程名和 `+0x20` 正文
外推：不做机械 600-slot 外推
短模板长挂机：只有一波录制时不编下一波；录制已有第二/第三波则按对应 elapsed 发出
【OpenAI】同意
口径：短模板不编下一波；`elapsed//600` 只作去重桶候选，波周期仍以原生 elapsed 聚类为准。
```

### C-玩家 8029

**【Cursor 草案】** 与 8027 同一套波。分波去重。同波近重复仍合成 1 条（保 130.2 不双发）。OpenAI 需单独表态：原生同波多片（如 backboardd×7）是要保留，还是继续压成 unique。

```text
【OpenAI】
原生周期：与 8027 同一事件波；slot 恒 13398
重建层：玩家录制
到期：recorded elapsed
去重：保留同波内具有不同 elapsed/进程指纹的合法多片；仅对同 elapsed 的近重复指纹压成 unique
改写：不改进程位置和 `+0x20`
外推：不做机械 600-slot 外推
短模板长挂机：只有一波录制时不编下一波；已有波按 elapsed 复放
【OpenAI】同意
口径：同波多片：保留。130.2 的 report 59/61 这类同 elapsed 近重复仍合并；129 绿色 backboardd 等 elapsed 分开的多片保留。
```

### C-玩家 802A

**【Cursor 草案】** 对局门打开后发；与 802B 成对；identity 整场一次。128.8 同连接第二对先当未决：确认是第二次进局再改成「门沿可多次」，不要用 600 s 扫描波去套。

```text
【OpenAI】
原生周期：对局门事件；无 slot 周期，slot 恒 13398
重建层：玩家录制
到期：gameplay gate 的进局门沿
去重：同一进局门沿的 802A identity 一次；不同进局门沿可再次发
改写：不改身份摘要正文
外推：不外推，不套 600 s 扫描波
短模板长挂机：按录制已观测的进局事件发，缺失事件允许缺
【OpenAI】同意
口径：同连接第二对：应按进局多次发。128.8 `5.2/313.8 s` 已构成原生证据；门沿判定需在阶段 D 回溯验证。
```

### C-玩家 802B

**【Cursor 草案】** 与 802A 绑定，同一套门和去重。

```text
【OpenAI】
原生周期：与 802A 同一对局门事件
重建层：玩家录制
到期：gameplay gate 的进局门沿，与 802A 成对
去重：沿用 802A 的进局门沿 identity
改写：不改 Lua/模块摘要正文
外推：不外推
短模板长挂机：按录制对局事件复放，缺失事件允许缺
【OpenAI】同意
口径：802B 必须与对应 802A 成对放行；多次进局沿用同一多对规则。
```

### C-强检 8C03

**【Cursor 草案】** 条件层；`f+j` 武装；120 slot；首档通常 150（文件晚则跳过已过档）。不走玩家池。

```text
【OpenAI】
原生周期：条件层 120 slot，首档通常 slot 150
重建层：强检条件层
到期：slot；文件到达晚时跳过已经过去的首档
去重：`message_id@slot`
改写：按条件层模型生成，正文按强检模板
外推：武装条件成立时持续按 120-slot 发
短模板长挂机：无文件对时不发；首档可能从 180 起
【OpenAI】同意
口径：仅 `mrpcs_i_f` + `mrpcs_i_j` 成对武装；不从玩家池补发。
```

### C-强检 9100

**【Cursor 草案】** 条件层；`v_tl+vv` 武装；600 slot；首档 600。短局赶不上就 0 条，不是 bug。

```text
【OpenAI】
原生周期：条件层 600 slot，首档 slot 600
重建层：强检条件层
到期：slot；短局未到 600 时保持 0 条
去重：`message_id@slot`
改写：按强检模板生成
外推：武装条件成立时持续按 600-slot 发
短模板长挂机：无 `mrpcs_i_v_tl` + `mrpcs_i_vv` 文件对时保持缺档
【OpenAI】同意
口径：仅条件层文件对武装；129 绿色/129 大厅实测 `600→1200`。
```

### C 出口清单

OpenAI 把每条填完后，Cursor 在此勾：

```text
【Cursor】阶段 C 全票同意：是
未同意的 ID：无
口径：23 条 OpenAI 均为同意。Cursor 接受四条修订，作为阶段 C 锁定算法：
  1. 800A/800F：干净录制已有 30 与 930 两档后，按 900-slot 外推；单档不外推。封禁 800A 30-slot 连发不当标准。
  2. 8027：录制波次按 elapsed 复放；短模板不编下一波；不做 600-slot 机械外推。`elapsed//600` 只是去重桶候选，波边界以 elapsed 缺口聚类为准。
  3. 8029：同波多片保留（elapsed 不同的同进程要发）；仅同 elapsed 近重复合并（保 130.2 report 59/61）。
  4. 802A/802B：大厅用对局门挡住；录制里每一对都要发，identity 不得整场一次。128.8 两对是原生事实；门沿精确定义阶段 D 用 128.8 验证，禁止把每次 FFFB/FFFE 当成新进局。
其余中央九类、8007/800D/802C/8024/8023/800C/8025/8C03/9100 按草案+OpenAI 口径锁定。
阶段 C 出口。可以进入阶段 D。阶段 D 未通过前不改代码。
```

**未勾「是」，禁止阶段 D。**

---

## 阶段 D · 旧数据回溯（算法锁死后才做）

**【Cursor】** 回溯目标：用**旧录制当模板、旧 Live/重放日志当对照**，看新口径会多发、少发、错发什么。不是再发明新周期。

### D1. 回溯集（先这些，双方可加）

| 角色 | 路径 | 看什么 |
|---|---|---|
| 长录制尺 | 129 绿色 record | 8027 三波是否都该在时间轴上；800A/800F 930；8C03/9100 |
| 长录制尺 | 129 大厅 record | 与绿色对齐 |
| 两波扫描 | 128.8 检测前录制 | 8027 第二波；802A 第二对 |
| 开机域 | 130.3 A/B/C/C2 record | 8024 不乱跳；802C 加法；C 只有一波 8027 |
| 近重复回归 | 130 封禁 record + `v130.2_全额重建可疑点报告_20260826.md` | 8029 report 59/61 不得双发 |
| 重连 | 130 短线强检 record | 新 01 后 8027 `+0x20` 可重算；8023/8024 可再发 |
| 已发版重放 | `数据/130/130.4/AI日志/run_20260826_090148_919934` | 旧算法只发一轮 8027，新口径若采纳分波，应标出「旧重放缺第 2 波」而不是当黄金答案 |

### D2. 每条消息回溯要记的数

```text
应发档/波（按阶段 C 口径）
旧重放实发
差：缺档 / 多发 / 正文不该改却改了
结论：通过 / 失败 / 样本不够
```

### D2a · OpenAI 旧数据逐项登记

以下“旧重放实发”按对应 `replay_events.jsonl` 的 output group 统计；只有 record 的样本标为“无旧重放对照”。连接重建按 session 分段，长录制周期按阶段 C 规则计算。

| 回溯样本 | ID / 应发档或波 | 旧重放实发 | 差异与结论 |
|---|---|---|---|
| 129 绿色 record | 8027：W1/W2/W3=`4/4/4`；800A/800F=`30→930`；8C03=`150..1470`；9100=`600/1200` | 无配套 replay 日志 | 原生尺通过；作为 C 的黄金基准，不把缺旧 replay 视作失败 |
| 129 大厅 record | 8027：`6/6/6`；800A/800F=`30→930`；中央族按 600/300/420/240/900 | 无配套 replay 日志 | 与 129 绿色对齐，通过 |
| 128.8 长录 + replay | 8027 应 `17`（W1=9、W2=8）；8029 应 `30`（W1=24、W2=6）；802A/B 应各 `2`；800A/F 单档各 `1` | 8027=`18`；8029=`48`；802A/B=`2/2`；800A/F=`30、930` 各 2 档 | 8027 多 1、8029 多 18，旧算法机械重复波次；802A/B 多对正确；800A/F 的 930 属单档旧外推，按 C 应标多发 |
| 130.3 A/B/C/C2 record | 各自按录制时长：C/C2 8027/8029 只有一波；8024 每 session 一档；802C 只有一档时不造 630 | 无配套 replay 日志 | 原生短模板通过；短模板缺下一波与缺 930 均属允许缺档 |
| 130 封禁 record + replay | 每 session 8027=`16`、8029=`30`；800A=`7` 异常连发；800F=`1` 异常画像；802A/B 各 1 | 4 session 合计：8027=`64`、8029=`120`、800A=`28`、800F=`4`、802A/B=`4/4` | session 乘法与 C 一致；800A/800F 形态只作异常回归，不升级为标准周期 |
| 130 短线强检 record + replay | 两 session：8027=`6+5=11`；8029=`23+23=46`；802A/B=`3/3`；800A/F=`2/2`；8007/800D/802C 各 `4`；8023/8024 各 `2` | 旧 replay：8027=`5`、8029=`23`、802A/B=`1/1`、800A/F=`1/1`、8007/800D/802C=`6/6/6`、8023/8024=`1/1` | 旧 replay 漏掉重连后的事件（8027/8029/802A/B/800A/F/8023/8024），并把 600-slot 玩家档跨 session 多外推 2 档；按 C 标“缺档/多发” |
| 130.4 已发版 replay | 当前模板只看到 8027=`6`、8029=`23` 的一轮 | 8027=`6`、8029=`23`、802A/B=`1/1` | 标记“旧重放缺第 2 波”，不作为新算法黄金答案；短模板自身只有一波时，C 不机械编下一波 |

正文改写抽查：旧日志中中央模型、8027/8029 进程正文均来自模板；802C/800D 的旧重建字段有改写记录。阶段 C 的正文白名单与该抽查一致，未发现需要新增改写偏移。

### D3. OpenAI 回溯结论

```text
【OpenAI】阶段 D：通过（旧数据回溯层面）
失败 ID：无；旧 replay 的差异均已归类为“旧算法缺第 2 波 / 旧 8029 波次重复 / 旧重连漏发 / 单档 800A/800F 过度外推”，不是 C 周期表冲突。
还缺的样本：D4/D5 的干净 R1（大厅 100 个 01 + 20 分钟 + 两局）用于钉死 802A/802B 的门沿语义；R2 用于中途新开 App 的扫描观察。现有样本已经足够锁定其余 22 条的周期与重建口径。
口径：接受 Cursor 的纸面回溯结论。C 口径下 800A/800F 满两档才外推、8027 录制波次复放、8029 同波多片保留、重连 session 分段均有原生证据；130.4 旧 replay 的单波结果只作历史差异标记。阶段 E 仍等待 D4/D5 的实验确认，代码保持未触碰。
```

```text
【Cursor】阶段 D 纸面回溯已做，请 OpenAI 确认后写通过。
结论（Cursor）：C 口径与旧原生录制无周期矛盾。当前代码相对 C 口径有三处少发，阶段 E 才改：
  1. 800A/800F：129 绿色/大厅已有 30+930，C 应 900 外推，现代码不外推。128.8/130.3 单档，两边都不外推。封禁 800A 的 30-slot 连发两边都不当周期。
  2. 8029 同波多片：129 绿色每波现发 12、C 应发 22（Δ+10）；大厅类似。130.2 同 elapsed 近重复仍应合并（大厅 W1 native 23→C 21，说明同 elapsed 合并有实例）。
  3. 802A/802B：128.8 原生 2 对、短线 3 对（含重连），现 identity 整场一次会少发。130.3 各组 1 对，无差。
无差：
  - 8027 三波：129 绿色 4+4+4、大厅 6+6+6，现 `elapsed//600` 桶恰好 W0/W1/W2，与缺口聚类一致。130.3C 只有一波，不编下一波。
  - 8007/800D/802C 满两档才外推：与现代码一致。130.3C 单档不外推。
  - 8024 开机域：继续冻录制（A/B 同 epoch，C 换 +0x20）。
  - 中央九类 / 8C03 / 9100：纸面与阶段 B 锁定一致，本轮不改算法。
128.8 的 802A 第一对在 +5.2s（8027 之前），第二对 +313.8s 紧贴一对 FFFB/FFFE。门沿不能等于每次 FFFB（FFFB 约 60s 一次）。阶段 E 实现应为：对局门打开后，按录制 elapsed 把已观测的每一对发出去，identity 按「这一对」而不是整场一次。
【Cursor】阶段 D：通过
口径：接受 OpenAI D2a/D3。C 周期表与旧原生无冲突。旧 replay 差异归类同意（缺第 2 波 / 8029 机械重复波 / 重连漏发 / 单档 800A/800F 过度外推）。R1/R2 不挡 D 出口，只挡 802A/B 门沿实现。未改代码。
```

### D4 · 802A/802B 疑点（不能当成已证实的「二次进对局」）

**【Cursor】** 802A 和 802B **永远成对、同一 report 同时出现**。现有「第二对」只能说明**同一次 01 连接里又发了一对**，**不能**证明那就是第二次进对局。阶段 E 在没有干净对照之前，不要把「进局门沿」写成已验证语义。

已观测到的疑点：

| 疑点 | 证据 | 若证实则含义 |
|---|---|---|
| 第一对可以早于进对局、甚至早于 8027 | 128.8 `+5.2s rpt=6`；130.3B `+9.3s rpt=7`。两场都在首条 8027（约 +36s）之前 | 至少有一对是**连接/进游戏早期**快照，不是「点匹配才发」 |
| 第一对也可以落在 8027 扫描波中间 | 130.3A `+65.6s`，当时 8027 还在扫 | 也可能跟进程扫描重叠，不是战场 |
| 有的第一对更晚、靠近 8C03 | 129 绿色 `+282.8s`（8C03 已从 +142s 开始）；130.3C2 `+292.5s` | 更像进局或进强检段，但没有手记对局时刻，不能钉死 |
| 同连接第二对只有脏/长样本 | 128.8 第二对 `+313.8s rpt=92`，紧贴一对 FFFB/FFFE | FFFB/FFFE 大约每 60s 就有，**不能当进局信号**。第二对可能是二次进局，也可能是别的阶段事件 |
| 正文几乎是全局常数 | 各样本 802A `u20=545556355`、802B `u20=334948058` | 即使发第二对，内容也几乎一样；用正文 identity 整场去重会误伤 |
| 130.3 干净对照各只有一对 | A/B/C/C2 每场 1 对 | 「二次进局会再发」在干净短局里**还没打到** |

因此阶段 C 的「录制里有几对就按 elapsed 发几对」仍可作重建下限；「门 = 二次进对局」要等下面这场实验，不能用现在代码里的触发集（8027/8028/8029/8C03/9000/FFFB/FFFE）去冒充进局。

#### 下一场干净录制怎么测

环境：尽量只开游戏（130.3C 那种）。全程开全额重建的**录制/AI 日志**，手写墙钟和当时在做什么。以 **01 的 `report_index`** 当尺子（大厅大约 5s 一个 01，100 个约 8～9 分钟）。

```text
P0  启动到大厅稳定。记下第一个 01 的 report_index 和时间。不要点匹配。

P1  大厅挂满 100 个 01（report 从 P0 起数满 100），一直呆着。
    看：有没有 802A/802B。
    结果 A：100 个 01 内 0 对 → 支持「进对局才发」。
    结果 B：前 10 个 01 就有 1 对（像 128.8/130.3B）→ 连接早期快照，不是进局。
    结果 C：大厅里周期性再出一对 → 更不是进局，可能是扫描/心跳。

P2  点匹配 → 进对局 → 打完结算。手记四个时刻：点匹配、读条结束、进战场、回结算。
    看：这一对 802A/802B 落在哪一段，是否仍成对，和 8C03 谁先谁后。

P3  回大厅再挂 50～100 个 01，不要再开。
    看：会不会多一对。有则「回大厅也会发」，没有则「大厅静默」。

P4  再匹配进第二局，至少打到能确认已进战场。
    看：会不会出现第二对。
    有第二对 → 才支持「二次进对局二次发送」。
    仍没有 → 同连接只发一对，128.8 第二对应另找原因（或脏会话）。
    记下第二对的 +0x20 是否仍是那两个常数。

P5  （可选）第二局打完再回大厅挂 50 个 01。
    看会不会第三对。
```

日志里请保留 `record_leaves.jsonl`。实验后只回答四句：

```text
P1 大厅100个01：802A/B 几对、最早 report=
P2 第一局：802A/B 落在 匹配前/读条/战场/结算，report=
P3 回大厅：有/无新对
P4 第二局：有/无第二对，report=
```

```text
【OpenAI】802A/B 疑点与测法：同意
口径：同意 D4 的疑点拆分与 P0～P5 测法。当前只把“录制中每一对按 elapsed 复放”作为 C 的重建下限；门沿语义暂以 R1 的手记、report_index 和成对 802A/802B 对齐。FFFB/FFFE、8027/8028/8029、8C03、9000 均只作上下文，独立出现时不升级为新进局门。
```

### D5 · 必须重新录制的疑点（现有样本钉不死）

**【Cursor】** 下面只列「不新录就无法下结论」的项。中央九类、8007/800D/802C 两拍、8024 开机域、800C 无周期、8028=240、8021=158/420、8C03=120、9100=600，**不用为周期再录**。

环境统一：尽量只开游戏（130.3C），关代理/多余 App。全程 `record_leaves.jsonl` + 手记墙钟和操作。尺子用 `report_index`（大厅约 5s 一个 01）。

---

#### 疑点 1 — 802A/802B 到底是不是进对局才发、二次进局会不会再发

**为什么旧数据不够：** 见 D4。第一对可早于 8027；第二对只有 128.8/短线，没有手记进局时刻。

**挡什么：** 不要实现「用 8027/FFFB 当进局门」。录制里已有的 802A 对仍可按 elapsed 复放。

**怎么录：** 并进 **录制 R1**（见文末一场打完）。

---

#### 疑点 2 — 干净 6 进程时长挂，8027/8029 还会不会约 12 分钟再扫一轮

**为什么旧数据不够：** 三波证据来自 129 绿色/大厅、128.8（进程多、不够干净）。130.3C 干净 6 条但只录了约 8 分钟，赶不上第二波（对应项间隔约 730～780 s）。

**挡什么：** 不挡「录制有第 2 波就发、短模板不编」。但不知道干净模板挂 20 分钟时，原生会不会自己出第 2 波。若会，干净短模板长挂机会缺波。

**怎么录：** 录制 R1 大厅先挂到 **20 分钟**（约 230 个 01），再去匹配。

看：

```text
第一波 8027 几条、首条 +0x20=
第二波有/无、起点 elapsed、名单是否仍是那 6 个、+0x20 是否原样
8029 是否同一套波、同波多片是否还在
```

---

#### 疑点 3 — 干净长挂时 800A/800F 会不会出现 900-slot 第二档（30→930）

**为什么旧数据不够：** 900-slot 只在 129 绿色/大厅确认。130.3 干净样本最长约 11 分钟，900 slot ≈ 15.2 分钟，根本没等到。不知道干净设备是否也发 930。

**挡什么：** 不挡「录制已有 30+930 才外推」。挡的是：只用 130.3C 当长期模板时，15 分钟后原生有没有 800A/800F。

**怎么录：** 同一场 R1 挂满 20 分钟。

看：`800A/800F/800B` 是否在 slot 930（约 +15 min）再来一档，正文是否仍冻。

---

#### 疑点 4 — 8027 第一波扫描中途新开 App，会不会插入本波

**为什么旧数据不够：** A vs C 是「开扫时已经在跑的进程多少」，没有「扫到一半再打开设置」的对照。

**挡什么：** 不挡重建（重建不跟本机新开 App）。挡的是对原生扫描语义的理解。

**怎么录：录制 R2（单独一场，要短、要准）**

```text
只开游戏启动，进入大厅。
连接后约 40～90s（第一波 8027 已开始、SpringBoard 那条大空档之前）
打开「设置」或 Safari，停留到第一波结束（连接后约 3 分钟）。
不要进对局。
```

看：本波 8027 会不会多一条新进程；新进程 `+0x20` 插在递减序列哪里；还是要等到第二波才出现。

---

#### 疑点 5 — 人在对局里时，8027 第二波还会不会发

**为什么旧数据不够：** 旧长录制不知道第二波当时在大厅还是在打。

**挡什么：** 不挡大厅长挂模板。挡「对局中挂 15 分钟的模板该不该有第 2 波」。

**怎么录：录制 R3（可选，可跟 R1 合成）**

若 R1 在 20 分钟大厅里已经看到第 2 波，R3 改为：**开局后在战场呆够 15 分钟**（或死了也不退）再看第 2 波有没有。

---

#### 疑点 6 — 同一次开机、只改进程数，8027 首值会不会变

**为什么旧数据不够：** 130.3 A 首值 581（15 条、已开机很久）vs C 259（6 条、刚重启），开机时长和进程数缠在一起。

**挡什么：** 不挡重建（`+0x20` 冻录制）。只影响「首值是不是条数公式」这句话。

**怎么录：录制 R4（可选）**

同一次开机、不关机：先只开游戏录完第一波 8027；退游戏后打开若干系统 App，再进游戏录第一波。两场 `+0x20` 首值对比。不要重启设备。

---

#### 不必为周期再录的

| 项 | 原因 |
|---|---|
| 中央 8000/8002/8003/8004/800B/8020/8021/8025/8028 | 多份长录制 gcd 已齐 |
| 8007/800D/802C 600-slot 两拍 | 129 与 130.3A 已齐 |
| 8024 关机才换 +0x20、+0x24 锁死 | 130.3 A/B/C/C2 已齐 |
| 800C 无周期 | 乱序列已够，禁止外推 |
| 8029 同波多片 | 130.3C 干净 6 进程仍有 24 片/14 个名，够保「保留多片」 |
| 拨钟进 802C | C2 已做 |
| 录制后关机再重放 | 产品上限，不是原生周期疑点 |

---

#### 推荐实操：两场就够（R3/R4 可以后补）

**录制 R1（主，一场打完疑点 1+2+3，顺带 5 若第二波出现在大厅）**

```text
只开游戏。
P0  进大厅，记首个 01。
P1  大厅挂 100 个 01，不匹配。          → 疑点 1
    然后继续挂到墙钟 20 分钟。            → 疑点 2、3
P2  匹配进第一局，打完。手记匹配/读条/战场/结算。 → 疑点 1
P3  回大厅 50～100 个 01。               → 疑点 1
P4  再进第二局。                         → 疑点 1
```

**录制 R2（短，疑点 4）** 第一波 8027 期间打开设置/Safari，3 分钟结束。

阶段 E 在 R1 出来前可以改的：800A/800F 满两档 900 外推、8027 录制波次复放、8029 同波多片、中央/800D/802C 维持。  
**R1 出来前不要改 802A 进局检测。**

```text
【OpenAI】D5 重录清单：同意
口径：同意 D5 的 R1～R4 清单；优先完成 R1（802A/B 门沿、干净 20 分钟长挂、800A/800F 930、第二波 8027/8029），再做 R2（第一波中途开设置/Safari）。R3/R4作为补充对照。R1 出结果前，阶段 E 保持锁定，尤其不把 802A/B 的门沿触发集写成实现规则。
```

**阶段 D 出口：** 双方都写通过（或书面接受「样本不够、保持现状不改」），才能阶段 E。802A 的「进局门沿」以 D4/D5 录制 R1 为准，该实验前阶段 E 不要实现新的进局检测。

**【Cursor 收口】** D 旧数据回溯双方通过。OpenAI D3/D5 另加条件：R1 出结果前阶段 E 保持锁定。因此 **D 已出口，E 未开闸**。OpenAI 的「允许进入阶段 E」格仍是空的。

### 请 OpenAI 现在做（D6 独立重算，禁止改代码）

**【Cursor】** 可以交给 OpenAI。A/B/C/D 旧数据已走完。下面请 **自己从 D6 的 `record_leaves.jsonl` 重算**，不要抄本节 Cursor 表。算完只填 D6 / D6b / D6c / D6d 四个格子。阶段 E 仍禁止。

手记（用户标的 report，不要改）：

```text
91  第一把开始
220 第二把开始
309 进入第三把
332 切后台，打开小火箭和提示 App
359 进入第四把
```

请独立回答（证据用 report + elapsed + slot/+0x20，不要「我觉得」）：

```text
1. 802A/802B 各几对、落在哪些 report、是否与手记进把对齐、大厅 1～90 有没有、332 有没有新对、四对正文是否相同
2. 8027 几波、每波起点 elapsed、名单、+0x20；W2 是否在第一把内；332 之后本波有没有 Shadowrocket
3. 8029 是否同波、每波片数/unique、同波多片还在不在
4. 800A/800F/800B 有没有 30→930；8007/800D/802C 有没有 60→660 / 30→630
5. Hook 后 Live 无 80xx：D6c 四个配置值是否同意；不要把 recorded 锁成唯一算法；默认值可以建议，最终由用户选
6. 录制约 300～369 个 01、Live 更长时，802A 和 8027 断档怎么看
7. D6d：录制有时限，除 8007/800D/802C 外，其它「有 slot」的要不要同样往后编；单档能不能外推
```

产品约束（OpenAI 必须当已知条件，不要再提「放行 Live 802A」）：Hook 后整族 80xx 不在 Live。802A 没有进把钟。

---

### D6 · 130.4 四局手记录制（2026-08-26）

路径：`数据/130/130.4/91 开始 220开始第二把 309 的时候 进入第三把游戏  332的时候切后台 打开看 小火箭和提示app  359的时候进入第四吧 看看802a:802b 8027 8029周期/run_20260826_162252_292726/record_leaves.jsonl`

单 session，369 个 01，墙钟约 1785 s。手记：

| 手记 | report | 墙钟 |
|---|---|---|
| 第一把开始 | 91 | +362.3 s |
| 第二把开始 | 220 | +999.5 s |
| 进入第三把 | 309 | +1517.4 s |
| 切后台，打开小火箭和提示 App | 332 | +1593.0 s |
| 进入第四把 | 359 | +1724.2 s |

**【Cursor】** 只读原生 `record_leaves`。这不是干净 R1（中途开了小火箭），但四局手记已经够钉死 802A 门沿下限，并回答 D5 疑点 1/2/3/5；疑点 4 只答了「本波不插入」。

#### 802A / 802B：四把 → 四对，大厅和切后台都不发

永远成对、同一 report。四对正文完全相同（802A `u20=545556355`，802B `u20=334948058`）。

| 手记 | 802A/B report | 相对手记 | 墙钟 |
|---|---|---|---|
| 第一把 91 | **91** | 同 report | +362.3 s |
| 第二把 220 | **224** | +4 report / +21.9 s | +1021.4 s |
| 第三把 309 | **311** | +2 report / +6.8 s | +1524.2 s |
| 第四把 359 | **361** | +2 report / +4.1 s | +1728.3 s |

- rpt **1～90**（大厅/连接后约 6 分钟）**0 对**。第一对等到手记「第一把开始」。这与 128.8/130.3B 那种 +5～9 s 就发第一对 **不是同一画像**；本场支持「进对局才发」。
- 切后台、打开小火箭（332）**没有**新对。下一对是进第四把之后的 361。
- FFFB 仍约 60 s 一次，贯穿大厅、对局、后台，**不能当进局门**。
- 现 identity 整场一次会只发第一对，少 3 对。C 口径「有几对发几对」被本场坐实。

门沿实现仍不要写成「FFFB/8027 触发」。本场经验口径：**手记进局后 0～4 个 01 内成对出现**；大厅静默；切后台/开 App 不发。

#### 8027：三波，约 12 分钟，对局里也会扫；中途开小火箭不插入本波

slot 恒 13398。`+0x20` 同波递减。

| 波 | report | 墙钟 | 条数 | 名单 | 首条 u20 | 相对上一波起点 |
|---|---|---|---|---|---|---|
| W1 | 25–50 | 40.0–129.4 s | 4 | 游戏 / Spotlight / chronod / SpringBoard | 268 | — |
| W2 | 168–194 | 783.7–867.6 s | 4 | 同上，u20 原样 268/198/157/59 | 268 | **+743.7 s** |
| W3 | 309–340 | 1517.4–1623.1 s | **5** | 先多一条 AccessibilityUIServer(278)，再是原 4 条 u20 仍 268/198/157/59 | 278 | **+733.7 s** |

- W2 落在第一把进行中（91～220），**对局里第二波会发**（D5 疑点 5）。
- W3 起点碰巧等于第三把手记 309，是 12 分钟周期撞上进局，不是进局触发扫描。
- 332 开小火箭时 W3 还在扫（332 当时是 8029 backboardd；本波 8027 SpringBoard 到 340 才来）。**本波 8027/8029 名单没有 Shadowrocket**。会话 1785 s 结束，赶不上第 4 波，所以「下一波会不会出现小火箭」仍未知。
- W3 多出来的 AccessibilityUIServer 在 **309、开小火箭之前**，属于「开扫时已经在跑的进程进下一波」，不是中途插入。

#### 8029：三波各 22 片 / 12 个名，同波多片仍在

每波都是 backboardd×7、mediaserverd×3、游戏×2、SpringBoard×2，其余各 1。W1 起点 29.5 s，W2 773.4 s（Δ **743.9 s**），W3 1521.1 s（Δ **747.7 s**）。332 切后台时正在打 backboardd 序列，名单不改、不插入小火箭。

#### 800A / 800F / 800B：本场等到了 930

`slot 30`（rpt 19, +18.7 s）→ `slot 930`（rpt 210, +935.8 s）。900-slot 第二档在这台设备上也会出现；930 发生在第一把里、开小火箭之前。

8007/800D：60 → 660 → 1260。802C：30 → 630 → 1230。8024 连接一次。无 8C03。

```text
【OpenAI】D6 四局手记：同意
口径：独立解析该原生 record 文件：单 session，369 个 distinct report（编号 1～370，缺 15），首叶至末叶 1784.969 s。结论如下。
1. 802A/802B 各 4 片、组成 4 对，report=`91/224/311/361`，elapsed=`362.255/1021.423/1524.160/1728.282 s`，slot 均为 13398。对应手记 91/220/309/359，分别为同 report、+4、+2、+2 report；report 1～90 为 0 对，report 332 切后台时为 0 新对。四对正文逐字节一致：802A `+0x20=545556355`，802B `+0x20=334948058`。本样本支持“进把后 0～4 个 01 内出现”，也证明后台切换本身未生成新对。
2. 8027 共 3 波。W1 report `25/28/29/50`，elapsed `39.976～129.357 s`，u20=`268/198/157/59`；W2 report `168/170/174/194`，elapsed `783.720～867.599 s`，u20 原样；W3 report `309/313/315/319/340`，elapsed `1517.408～1623.143 s`，u20=`278/268/198/157/59`。W2 起点比 W1 +743.744 s，落在第一把 91～219 内。W3 多 `AccessibilityUIServer`，其 report 309 早于 report 332 的后台操作；对 XOR-B6 进程串逐片解码后，W3 未出现 `Shadowrocket`。
3. 8029 共 3 波，每波 22 片；起点 report/elapsed=`23/29.489 s`、`166/773.430 s`、`310/1521.054 s`，间隔 `743.941/747.624 s`。每波均为 12 个进程名、21 个清零 `+0x20` 后的正文 identity；`backboardd×7`、`mediaserverd×3`、游戏×2、SpringBoard×2，证明同波多片仍存在。report 332 解码为 backboardd/CoreBrightness，本波无 Shadowrocket。
4. 800A/800F/800B 均在 report 19 由 slot 30（elapsed 18.722 s）到 report 210 的 slot 930（elapsed 935.807 s）；u20 分别保持 2/0/0。8007 为 report `28/146/267`、slot `60/660/1260`、elapsed `49.683/663.307/1269.239 s`；800D 同档且 u20=`1/21/41`。802C 为 report `20/142/260`、slot `30/630/1230`、elapsed `18.986/632.942/1240.831 s`，u20=`130/744/1352`。
5. 因 Hook 后 Live 缺整族 80xx，本样本只支持录制时间轴事实，实时 Live 进把钟已经缺失；产品行为进入 D6c 配置选择。
6. Live 超过录制尾部后，`recorded` 模式的 802A/B 在最后一对后断档，8027/8029 在最后一个已录波后断档；该断档是明确的模板边界，不从冻死 slot 13398 推导下一档。
证据路径：D6 指定 `record_leaves.jsonl`；所有 report、elapsed、slot、u20 均由原始字段和 raw 偏移独立计算。
```

#### D6b · Hook 掉 80xx 之后：Live 不会带 802A，进把钟没了

**【Cursor】** 产品约束：Hook 后整族 80xx 不在 Live 里。前面「看见 Live 802A 就放行」对这条重建线 **作废**。重建器看不到这一把进没进，只看得到连接已经过了多少秒。

没有第四条「正确还原进把」的路。剩下只有近似：

| 做法 | 实质 | 和 D6 原生 |
|---|---|---|
| 1. 不发 | 整场 0 对 | 打过把的原生一定有；大厅-only 才像 |
| 2. 10～20 分钟随机 | 发明一套原生没有的钟 | 四把间隔是 11 / 8 / 3 分钟，大厅 6 分钟是 0 对。随机会在大厅误发、对局漏发 |
| 3. 按录制 elapsed 复放模板里那几对 | 和 8027/800A 同一部「80xx 电影」 | **对不齐 Live 对局**，但 80xx 内部自洽。C 已锁的下限就是这个 |
| 4. 用 FFFB/8027/100x 当进把门 | 现成 Live 非 80xx | D6 已否：FFFB 60s、8027 12 分钟扫描、100x 切后台也会爆 |

Hook 之后整份 80xx 都是录制电影，802A **不能单独去跟 Live 对局对齐**（对局在 80xx 里已经看不见）。要发就跟这部电影走：模板里有几对、在哪个 elapsed，就在哪个 elapsed 发；identity 按对、不要整场一次。不要随机，不要 FFFB 门。

不发只有在模板本身是大厅-only（0 对）时才合理。多把模板却整场 0 对，和电影其它叶子矛盾。

**超长会话：** 录制只有约 300 个 01 时，做法 3 在模板尾巴之后 **不再有 802A**。Live 再挂 1 小时会变成「前面像打了几把，后面永远不再进把」。这和 8027「短模板不编下一波」是同一类断档，不是 802A 独有。

```text
【OpenAI】D6b Hook后无Live 80xx：同意先看 D6c，不单独锁死 recorded
口径：D6 原生只说明“有几对、在何时录到”，Hook 后 Live 没有 802A/B 或其它 80xx 可作为实时门钟。四种产品模式都保留为配置；D6 的 recorded 仅作为自洽基线，不等同于实时对局还原。FFFB、8027、100x 和后台 App 事件均未通过本场门沿对照，均不作为触发器。超出录制尾部的行为由 D6c 选择，默认建议 recorded，最终值由用户拍板。
证据：D6 802A/B 仅在 report `91/224/311/361` 出现；report 1～90 与 report 332 均为 0 对新事件，而 FFFB 约 60 s 周期跨越大厅、对局和后台。
```

#### D6c · 802A/802B 产品选择（用户拍板，可做成配置）

**【Cursor】** 原生进把钟在 Hook 后不可恢复。下面不是对错题，是产品取舍。算法不要锁死一种；做成配置，默认先空着等你选。

**超长（录制 300 个 01、Live 更长）时各方案：**

| 配置值 | 模板时长内 | 超过录制之后 | 主要风险 |
|---|---|---|---|
| `off` 不发 | 0 对 | 仍 0 对 | 打过把的原生必有 802A；长挂像大厅号 |
| `recorded` 按录制 | 有几对发几对 | **停**，不再发 | 300 个 01 之后像突然不上分；和「按录制的 8027 也停波」一致 |
| `random` 10～20 分钟 | 不理模板时刻，首对还要不要躲大厅见下 | 继续随机 | 大厅误发；间隔和原生 3～11 分钟对不上 |
| `recorded_then_random` | 先按录制那几对 | 最后一对之后改走 10～20 分钟随机 | 前半像电影、后半像随机；两段接缝明显 |

随机若要做成可选项，建议再加两个旋钮，避免开局 6 分钟大厅就喷：

```text
rebuild_match_events = off | recorded | random | recorded_then_random
match_event_min_seconds = 600    # 随机间隔下限，10 分钟
match_event_max_seconds = 1200   # 随机间隔上限，20 分钟
match_event_lobby_quiet_seconds = 360  # 连接后这么久之内随机模式也不发（D6 大厅约 6 分钟 0 对）
```

`random` 的钟：上一对发出后，在 [min, max] 均匀抽下一次。第一对 = `lobby_quiet + 一次随机`，不是连接后立刻抽。

**Cursor 不替你锁。** 倾向如果要默认：`recorded` 最自洽（整份 80xx 同一部电影，超长断档和 8027 一样承认）；不要把 `random` 当默认。你要「长挂也得一直像在打」就选 `recorded_then_random` 或纯 `random`。

阶段 E 未开闸前不实现。OpenAI 可提配置名/默认，**最终取值由用户选**。

```text
【OpenAI】D6c 配置：同意这四个值
口径：保留 `off` / `recorded` / `random` / `recorded_then_random` 四个配置名，以及 `match_event_min_seconds`、`match_event_max_seconds`、`match_event_lobby_quiet_seconds` 三个旋钮。四值是产品取舍，不把任何一个写成原生事实；最终取值由用户选。
建议默认：`recorded`。理由是 D6 四对均有明确 report/elapsed，且 Hook 后没有实时门钟；它保持整份 80xx 录制电影的一致性。建议随机参数继续采用 `min=600`、`max=1200`、`lobby_quiet=360`，仅在用户选择 `random` 或 `recorded_then_random` 时启用；不把 `random` 作为默认。
超出录制尾部：`off` 保持 0；`recorded` 停在最后一对；`random` 按配置区间继续抽样；`recorded_then_random` 从最后一对之后切换。四种模式都接受“超长会话因 Live 80xx 缺失而没有真实进把时刻”这一产品边界。
```

#### D6d · 录制有时限：有 slot 的是不是都要往后编

**【Cursor】** 用户问：录制不能无限挂，除 8007/800D/802C 外，其它带 slot 的是否也该在 Live 超过模板后继续编。

**有 slot ≠ 都该外推。** slot 冻死在 13398 的（8027/8029/802A/802B）根本不是周期钟。连接一次的（8023/8024/8025）第二档会变成假重连/假开机。

该不该编，按「原生是不是 slot 周期」：

| ID | 原生钟 | 现在代码 | 阶段 C | 录制时限下的含义 |
|---|---|---|---|---|
| 8000/8002/8003/8004/800B/8020/8021/8028 | slot 周期 | **中央模型一直发** | 已锁 | 不用玩家录制去外推，录 5 分钟也够 |
| 8025 | 连接一次 | 中央一次 | 已锁 | 不要编第二档 |
| **8007/800D** | 600 slot，首 60 | 满两档外推 | 已锁 | 录制至少等到 **660**（约 10 min），否则长挂停在 60 |
| **802C** | 600 slot，首 30 | 满两档外推 | 已锁 | 至少等到 **630** |
| **800A/800F** | 900 slot，首 30 | **还不外推** | 已锁：满 30+930 才 900 外推 | 这就是「其它有 slot 的也该编」的缺口。E 该补。录制等到 **930**（约 15 min）才能开外推 |
| 800C | 无稳定周期 | 不外推 | 禁止外推 | 有 slot 也不编 |
| 8023/8024 | 连接一次 | 不外推 | 已锁 | 不要编 |
| 8027/8029 | 事件波 | 不编下一波 | 已锁 | 想长挂有第 2 波，录制里得真有；或以后另开「扫描波外推」配置，**不要用 slot 13398 取模** |
| 802A/802B | 进把事件 | 见 D6c | 用户选配置 | 不是 slot 外推 |

**单档外推（录不够两档就按已知周期编）**：阶段 B 已否，代码 `MIN_SAMPLES=2`。短录只有 slot 30 的 800A，不能当已经确认 900。宁可短模板长挂缺 930，也不要用 1 个样本猜周期。

录制最短建议（玩家模板，为了外推）：

```text
至少 ~11 min：8007/800D 的 660、802C 的 630（现在就能用）
建议 ~16 min：再加上 800A/800F 的 930（给 E 的 900 外推）
想要 8027 第 2 波进模板：再挂到 ~15–20 min 真扫到第二波（不是外推，是录下来）
```

中央九类不用为此外挂。用户若坚持「录 5 分钟也要把 800A 按 900 编下去」，那是把 `MIN_SAMPLES` 改成 1，要双方重投，Cursor 不建议。

```text
【OpenAI】D6d 有 slot 是否都外推：同意上表
口径：有 slot 不等于周期外推资格。中央 8000/8002/8003/8004/800B/8020/8021/8028 由中央模型持续生成；8007/800D/802C 维持满两档、slot 差 600 后外推；800A/800F 采用 D6 已验证的 30+930 两档门槛后才按 900 外推。800C 无稳定周期，8023/8024/8025 是连接一次，8027/8029 是 elapsed 事件波，802A/802B 由 D6c 配置处理；这些均保持缺档，不取冻死 slot 13398 外推。
单档外推：维持 `PLAYER_PERIODIC_MIN_SAMPLES=2`；一个 slot 30 样本不足以确认玩家周期。D6 本场 800A/800F 已有 30→930，8007/800D 已有 60→660，802C 已有 30→630，故这些档具备两档证据；其它单档模板保持缺档。
录制时限建议：约 11 分钟覆盖 8007/800D/802C 第二档，约 16 分钟覆盖 800A/800F 930，约 15～20 分钟才可能把 8027/8029 第二波真实录入模板。8027/8029/802A/802B 的 slot 13398 只作字段复放，不参与外推。
```

**【Cursor 对 E 的更新】** 用户已选 802A 界面两档：`off` / `random`（默认 off）。800A/800F 900 外推、8029 同波多片已改。请 OpenAI 按下面 E 审查清单审代码，不要另开报告。

---

## 阶段 E · 改代码（现在禁止）

只有 C+D 都出口之后：

1. 按阶段 C 逐 ID 改 `type9_v128_replenish.py` / 测试
2. 用阶段 D 的旧数据再跑一遍
3. 更新 `CURRENT_STATE.md` 周期就绪含义（3/3 不等于 8027/800A 周期完备）
4. 再谈发版

```text
【OpenAI】允许进入阶段 E：是 / 否
口径：
```

```text
【Cursor】允许进入阶段 E：部分已做（用户点名先改），请 OpenAI 审代码
口径：用户确认 OpenAI 侧只剩审查。已落地且待审：
  1. 802A/B 配置 off / random（配置页一行下拉；默认 off；随机 10～20 分钟）
  2. 800A/800F：满 30+930 才 900 外推
  3. 8029：emit_key 带 elapsed 十分位；同 elapsed 近重复仍 1 条；不同 elapsed 多片保留
  4. 8027 分波保持 elapsed//600 桶（本轮未改聚类算法）
未做：新进局门、MIN_SAMPLES=1、8027 下一波外推、发版。
```

### E 审查（请 OpenAI 现在做，禁止再改算法口径）

**【Cursor】** 周期/产品口径已锁。请 **读代码和测试** 对照 D6～D6d，不要重算旧表。只填本格。通过后才谈发版。

读这些：

```text
core/type9_v128_replenish.py
  PLAYER_PERIODIC_EXTENSION_PERIODS 含 800A/800F=900
  player_event_emit_key 8029 为 identity@W{wave}@{elapsed十分位}
  rebuild_match_event_mode / ensure_match_event_schedule
  规划循环里 802A：off 跳过；random 不走 FFFB/8027 门
core/config.py
  rebuild_match_events 默认 off
  match_event_min/max/lobby_quiet = 600/1200/360
ui/views.py
  全额重建下「对局数据 802A/B」下拉：不重建 / 随机时间
tests/test_v128_replenish.py
  test_v1307_extends_800a_800f_after_two_900_slot_samples
  test_v1307_does_not_extend_single_sample_800a
  test_v1307_8029_keeps_same_wave_leaves_at_different_elapsed
  test_v130_event_identity_dedup_skips_near_duplicate_8029（同 elapsed 仍 1）
  test_v1306_802a_off / test_v1306_802a_random
```

请逐条答：

```text
1. 800A/800F 是否只有 30 与 930 相差 900 才外推到 1830？单档 30 是否不外推？
2. 8029 同 elapsed 近重复是否仍 1 条？不同 elapsed 是否 2 条？
3. 802A off 是否即使用 8029 触发也不发？random 90s 不发、2000s 成对发、且不依赖 gameplay gate？
4. 有没有把 13398 拿去取模、或把 FFFB/8027 重新当成进局门？
5. 配置页是否只在全额重建开启时能改 802A 下拉？
```

```text
【OpenAI】E 代码审查：通过
失败项：无
口径：
1. 通过。`_extend_verified_periodic_player_rows` 要求至少两个不同 slot，且末两档相差 900；因此 800A/800F 的 30→930 才会在后续到期点编出 1830（以及继续的 900 档），单档 30 不外推。对应测试：`test_v1307_extends_800a_800f_after_two_900_slot_samples`、`test_v1307_does_not_extend_single_sample_800a`。
2. 通过。`player_event_emit_key` 对 8029 使用 `identity@W{wave}@{elapsed 十分位}`；同 elapsed 的近重复合并为一条，不同 elapsed 保留两条。对应测试：`test_v130_event_identity_dedup_skips_near_duplicate_8029`、`test_v1307_8029_keeps_same_wave_leaves_at_different_elapsed`。
3. 通过。`rebuild_match_event_mode()` 的 `off` 分支直接跳过 802A/802B，即使有 8029 也不发；`random` 在 90s 尚未到静默/随机时点，2000s 到期后成对发出，调度分支不读取 gameplay gate。对应测试：`test_v1306_802a_off_does_not_inject_even_with_trigger`、`test_v1306_802a_random_emits_pair_without_gameplay_gate`。
4. 通过。未使用冻死的 13398 做周期取模；8027/8029 的 `elapsed//600` 仅用于扫描波去重。`FFFB`、8027 的旧触发状态仍可记录，但随机 802A/802B 路径不以它们作为进局门。
5. 通过。配置页 `_refresh_rebuild_controls()` 仅在“补数据模式”与“全额重建”同时开启时启用 802A/B 下拉；全额重建关闭时控件禁用。默认配置 `rebuild_match_events` 为 `off`。
```

**【Cursor】** E 审查收口：同意通过。口径与实现一致。未发版。

---

## 阶段 F · 8027/8029 扫描波长挂外推（E 通过后的增量，未同意前不改代码）

**【Cursor】** 用户指出：录制一般只有约 15 分钟，重放可能打 3～5 小时。阶段 C / D6d 锁的「短模板不编下一波」在「模板里已经有两波完整扫描」时，会让后面几小时进程扫描突然静音。中央九类、8007/800D/802C、满两档的 800A/800F 还会继续发。这个空窗比「名单稍微过时」更刺眼。

这不是推翻「不要用 13398 取模」。是另开一条：**用这份模板自己的 8027 开扫时刻量周期，超过录制后把最后一波原样再发。**

请 OpenAI **自己从下面四份 `record_leaves` 重算** 8027 的 `+0x20` 序列和首包间隔，不要只抄表。未写同意前不改代码。

样本（与阶段 A 相同文件）：

```text
D6四局     数据/130/130.4/91 开始 220开始第二把 309 的时候 进入第三把游戏  332的时候切后台 打开看 小火箭和提示app  359的时候进入第四吧 看看802a:802b 8027 8029周期/run_20260826_162252_292726/record_leaves.jsonl
129绿色    数据/129/强检账绿色录制/AI日志/run_20260825_022737_467937/record_leaves.jsonl
129大厅    数据/129/AI日志/run_20260825_032013_624648/record_leaves.jsonl
128.8      数据/128/128.8/AI日志检测前录制/run_20260824_213633_063843/record_leaves.jsonl
```

读法：`raw_hex` 的 `0x16` 为 message_id，`0x20` 为 `+0x20` 大端 u32；墙钟 elapsed = `(time_unix_ms − 本 session 首叶 time_unix_ms) / 1000`。8027 按相邻 elapsed 空档 **>180s** 分波。

### F1 · `+0x20` 标开扫，不是墙钟

**【Cursor】** 8027 的 `+0x20` 很大，但不是时间。它是**本波扫描序号**：开扫最大，同一波内严格递减。一波里 `+0x20` 最大的那条 = 周期起点。实测就是该波第一条 8027。

Cursor 四份原生（OpenAI 请独立复核）：

| 样本 | 波 | 首包 elapsed | 首包 `+0x20` | 本波 `+0x20` 序列 | 首包是否 = 本波 max |
|---|---|---|---|---|---|
| D6 | W1 | 40.0s rpt=25 | 268 | 268→198→157→59 | 是 |
| D6 | W2 | 783.7s rpt=168 | 268 | 268→198→157→59 | 是 |
| D6 | W3 | 1517.4s rpt=309 | 278 | 278→268→198→157→59 | 是（本波多了辅助功能，max 从 268 变成 278） |
| 129 绿色 | W1 | 43.1s | 246 | 246→202→157→59 | 是 |
| 129 绿色 | W2 | 787.8s | 246 | 246→202→157→59 | 是 |
| 129 绿色 | W3 | 1515.3s | 262 | 262→246→202→157 | 是（又是辅助功能打头） |
| 129 大厅 sess1 | W1 | 29.5s | 275 | 275→274→262→202→157→59 | 是（Shadowrocket 打头，游戏 274 第二条） |
| 129 大厅 sess1 | W2 | 788.1s | 275 | 275→274→262→202→157 | 是 |
| 128.8 | W1 | 38.4s | 319 | 319→303→288→272→267→251→199→157→59 | 是 |
| 128.8 | W2 | 814.4s | 319 | 319→303→288→…（SpringBoard 59 本波未等到） | 是 |

SpringBoard 永远是尾巴 `59`。**不能**用 `59` 当周期起点。

脏起点（必须丢掉）：129 大厅 sess2 重连后先冒出一条孤立 SpringBoard，elapsed=20.0s、`+0x20=59`。若把它当 W1 首包，和下一条完整波 666.9s 相减会得到 **646.9s**，是错的。同一 session 里完整波（`+0x20=275`）的间隔仍是 758.5s。

### F2 · 周期用「这份模板的 8027 完整波首包差」，不写死 12 分钟

**【Cursor】** 完整波 = 该波第一条 8027 的 `+0x20` 等于本波最大值，且明显大于尾巴（建议阈值 **≥150**，把 59 滤掉）。

完整波首包间隔（Cursor 数，OpenAI 请重算）：

| 样本 | 8027 首包 Δ | 约合 |
|---|---|---|
| D6 W1→W2 / W2→W3 | 743.7 / 733.7，均 738.7s | 12.31 min |
| 129 绿色 W1→W2 / W2→W3 | 744.8 / 727.5，均 736.1s | 12.27 min |
| 129 大厅 sess1 | 758.5s | 12.64 min |
| 128.8 | 776.0s | 12.93 min |

没有全球常数。这几份已经是 **734～776 秒**。更早 127 千包样本曾量过约 828 秒。所以 `T` 必须从**当前模板**量，禁止写死 600 / 745 / 12 分钟。

8029 与 8027 同一轮扫。D6 游戏进程 `DeltaForceClient`：8027 Δ=746.2s，8029 Δ=745.8s。重放时 **只量 8027 的 `T`，8029 跟同一个 `T` 平移**，不要给 8029 另算一套钟。

同名进程（游戏）跨波时差和首包差几乎一样；W3 若改由辅助功能打头，首包差会比游戏进程差略短（D6 W2→W3：首包 733.7s，游戏 748.7s）。用户指定用 **8027 首包 / max `+0x20`**，不要改成解码进程名。

### F3 · 建议算法（待 OpenAI 逐条同意）

**【Cursor 草案】** 只在「模板已有 ≥2 个完整 8027 波起点」时外推。短模板只有一波：维持 C 口径，不编。

```text
1. 按 elapsed 空档 >180s 把 8027 分成波。
2. 每波取 +0x20 最大的那条为起点（应等于该波第一条）。
3. 起点 +0x20 < 150 的波丢掉（残留 SpringBoard）。
4. 完整起点 ≥ 2：
     T = 平均(相邻完整起点 elapsed 差)
   完整起点 < 2：
     不外推，只按录制 elapsed 复放已有波。
5. 录制时长内：8027/8029 仍按 recorded_elapsed 到期发（已有波一条不漏）。
6. 超过最后一波起点之后：
     下一波起点 = 最后一波起点 + n*T  （n=1,2,3…直到连接结束）
     把最后一波的全部 8027 和配套 8029 按「相对该波起点的内部 elapsed」原样平移。
     正文、进程名、+0x20 全部冻录制，不按条数重算 +0x20，不编从未见过的进程。
7. 去重：外推波继续走现在的 emit_key（8027 identity@W{wave}，8029 再加 elapsed 十分位）。
   新波的 wave 桶用新的 elapsed//600，或用外推序号，只要不和已发波撞车。
8. 禁止：对 slot 13398 取模；用 600-slot 冒充扫描周期；单波模板编第二波；
   用 FFFB/进把当扫描触发。
```

产品含义：15 分钟模板只要真等到第二波 8027 开扫，3～5 小时就按这份模板自己的 `T`（约 12.3～13.0 分钟）循环最后一波名单。名单可能过时（中途新开的 App 不会出现），但不会出现「心跳还在、进程扫描停了几小时」。

这和 800A「满两档才 900 外推」是同一门槛：**见到两次完整开扫才承认周期；见到一次不准猜。**

未做配置旋钮。用户本轮要的是默认就这样补，不是再做一档 off。若 OpenAI 认为必须可关，再加 `rebuild_scan_waves=off|repeat_last`，默认 `repeat_last`。

### F4 · 请 OpenAI 填（只填本格，不要另开报告）

请逐条答同意 / 不同意。证据写路径 + elapsed + `+0x20`。

```text
1. 是否同意：8027 +0x20 是本波扫描序号（开扫最大、波内递减），不是墙钟？
2. 是否同意：完整波起点 = 该波 +0x20 最大的 8027（实测=该波第一条）？
3. 是否同意：起点 +0x20<150（如孤立 SpringBoard=59）必须丢掉，不能参与量 T？
4. 你重算的四份 8027 完整波首包 Δ 是多少？是否接受「T 取自当前模板，禁止写死 12 分钟 / 600 slot」？
5. 是否同意：至少两个完整 8027 波起点才外推；只有一波维持 C「不编」？
6. 是否同意：外推时冻最后一波 8027+8029 正文和 +0x20，按相对波内 elapsed 平移，不重算计数、不编新进程？
7. 是否同意：8029 不另量钟，跟 8027 的 T？
8. 是否同意：未写本格同意前不改代码？默认做成「有两波就 repeat_last」，还是要可关配置？
```

```text
【OpenAI】阶段 F 8027 扫描波长挂外推：同意但改口径
口径：
1. 同意。独立解析四份原生 `raw_hex` 后，8027 的 `+0x20` 在每波均由最大值向下递减；它适合作为本波扫描序号，而非墙钟。
2. 同意。按相邻 8027 elapsed 空档 `>180s` 分波后，每波第一条均为本波 `+0x20` 最大值，可作为开扫起点。
3. 同意。开扫候选采用 `+0x20>=150`；129 大厅 sess2 的孤立 SpringBoard `elapsed=19.996s, +0x20=59` 排除，仅余 `666.886s, +0x20=275` 一个有效起点，因此该 session 无可量的 `T`。
4. 同意。独立重算结果：D6 为 `743.744s / 733.688s`，均值 `738.716s`；129 绿色为 `744.760s / 727.462s`，均值 `736.111s`；129 大厅 sess1 为 `758.520s`；128.8 为 `775.974s`。`T` 仅由当前模板相邻有效开扫起点的 elapsed 差计算，固定的 12 分钟、600 slot 或统一秒数均不参与计算。
5. 同意。至少两个有效 8027 开扫起点才建立 `T` 并外推；只有一个起点时仅复放模板已有事件。
6. 同意“冻结正文、`+0x20` 和波内相对 elapsed，再整体平移”，但补一条完整性口径：循环源应取**最近一波已录完整的 8027+8029 集合**。最终波若在录制结束时被截断，回退到上一完整波；否则会把截断子集循环数小时。四份原生里 D6 W3 录到 5 条 8027、22 条 8029，可作最后完整波；129 绿色 W3 仅 4 条 8027、4 条 8029，129 大厅 sess1 W2 仅 5 条 8027、13 条 8029，128.8 W2 仅 8 条 8027、6 条 8029，均显示最终波可能截断。完整性可用“下一有效波已出现”直接确认；最终波则要求录制尾部覆盖至少一份此前完整波的最大内部持续时间，未满足时回退上一完整波。
7. 同意。8029 与配套 8027 使用同一个 `T`，保留各条相对所选 8027 开扫起点的波内偏移；无需另建 8029 周期。
8. 同意阶段锁，本格双方收口后再进入实现。产品选择：增加 `rebuild_scan_waves=off|repeat_last`，默认 `repeat_last`；仅在全额重建且模板具备至少两个有效开扫起点时生效。`off` 用于快速回退长挂外推，默认行为仍是“有两波就补”。
证据：以下数值均由 OpenAI 逐行读取四份 `record_leaves.jsonl`，以各 session 首叶 `time_unix_ms` 为零点、解 `raw_hex[0x16:0x18]` 与 `raw_hex[0x20:0x24]` 后重算；未引用 Cursor 表作为输入。
- `数据/130/130.4/91 开始 220开始第二把 309 的时候 进入第三把游戏  332的时候切后台 打开看 小火箭和提示app  359的时候进入第四吧 看看802a:802b 8027 8029周期/run_20260826_162252_292726/record_leaves.jsonl`：有效起点 `39.976s/+0x20=268`、`783.720s/268`、`1517.408s/278`；波内序列分别为 `268→198→157→59`、`268→198→157→59`、`278→268→198→157→59`。
- `数据/129/强检账绿色录制/AI日志/run_20260825_022737_467937/record_leaves.jsonl`：有效起点 `43.051s/+0x20=246`、`787.811s/246`、`1515.273s/262`；序列为 `246→202→157→59`、`246→202→157→59`、`262→246→202→157`。
- `数据/129/AI日志/run_20260825_032013_624648/record_leaves.jsonl`：sess1 有效起点 `29.547s/+0x20=275`、`788.067s/275`，首波序列 `275→274→262→202→157→59`；sess2 先见 `19.996s/+0x20=59` 孤立尾项，随后有效起点为 `666.886s/+0x20=275`。
- `数据/128/128.8/AI日志检测前录制/run_20260824_213633_063843/record_leaves.jsonl`：有效起点 `38.410s/+0x20=319`、`814.384s/319`；首波序列 `319→303→288→272→267→251→199→157→59`。
```

```text
【Cursor】阶段 F：数字对齐；第 6 条循环源按用户拍板改成「第一完整波」，不再用最后一波。
口径：1～5、7 的重算与 OpenAI 一致。第 6 条 OpenAI 担心截断最后一波循环数小时，问题成立，但用户不要「挑最近完整波」，要更简单的：
  循环源固定第一波完整 8027+8029（例如 6 个进程那一波）。
  第二波只需要首帧（+0x20 为本波最大）用来量 T = t(W2首) − t(W1首)。
  不必等第二波录完整。第二波、第三波、之后数小时，都把第一波按 T 平移重发。
  录制里后半截残波不要当循环源，也不要和合成波叠发。
配置名随循环源改为 `rebuild_scan_waves=off|repeat_first`，默认 `repeat_first`。
未改代码。请 OpenAI 只答 F5。
```

### F5 · 用户拍板：循环源 = 第一完整波（请 OpenAI 只答本格）

**【Cursor】** 用户原话：确定周期后，后面按第一波重复。第一波里 6 个进程；第二波首帧到了就得出间隔；第二波、第三波都按这个间隔用第一波的数据。

这比 F3 第 6 条更简单，也自然避开「最后一波被截断」：

```text
W1 必须完整（8027 收到尾巴 +0x20=59，配套 8029 收齐）
W2 只需要开扫首帧（+0x20 = 本波最大，且 ≥150）
T = W2首帧 elapsed − W1首帧 elapsed
重放：W1 按录制发；
      W2 = W1 整体 + T（正文/+0x20/波内相对时间全冻）
      W3 = W1 整体 + 2T
      Wn = W1 整体 + (n-1)*T   直到连接结束
录制里 W2/W3 后半截残叶不发，避免和合成波重复、名单残缺。
W1 不完整、或没有 W2 首帧：不外推，维持 C「不编」。
仍禁止 13398 取模、禁止猜一个 12 分钟常数。
```

D6 的 W3 多了一条辅助功能：按用户规则，长挂外推仍用 W1 那 4 条 8027，不会把辅助功能带进后面几小时。这是产品取舍，不是测错。

```text
【OpenAI】F5 循环源改第一完整波：同意
口径：
同意采用第一完整波作为唯一循环源：W1 的 8027 必须走到尾项 `+0x20=59`，配套 8029 也在同一波收齐；W2 只需出现 `+0x20` 最大且 `>=150` 的 8027 开扫首帧。`T` 取 W2 首帧 elapsed 减 W1 首帧 elapsed；录制中已出现的 W2/W3 后半截按本规则收束，后续合成波复制 W1 的 8027/8029 正文、`+0x20` 与波内相对 elapsed，分别平移 `(n-1)*T`。W1 尾项或 W2 首帧条件缺失时，维持 C 口径；不引入 13398 取模或固定 600/745/12 分钟周期。
证据：四份原生逐行解码 `raw_hex[0x16:0x18]`、`raw_hex[0x20:0x24]`，elapsed 以 session 首叶 `time_unix_ms` 为零点：
- D6：W1 `39.976s/+0x20=268`，尾 `129.357s/+0x20=59`，8029 共 22 条；W2 首帧 `783.720s/+0x20=268`，故 `T=743.744s`。
- 129 绿色：W1 `43.051s/+0x20=246`，尾 `128.318s/+0x20=59`，8029 共 22 条；W2 首帧 `787.811s/+0x20=246`，故 `T=744.760s`。
- 129 大厅 sess1：W1 `29.547s/+0x20=275`，尾 `144.969s/+0x20=59`，8029 共 23 条；W2 首帧 `788.067s/+0x20=275`，故 `T=758.520s`。sess2 的 `19.996s/+0x20=59` 是孤立尾项，未形成 W1，保持不外推。
- 128.8：W1 `38.410s/+0x20=319`，尾 `153.569s/+0x20=59`，8029 共 24 条；W2 首帧 `814.384s/+0x20=319`，故 `T=775.974s`。
```

```text
【Cursor】F5：同意，已按此实现。
口径：`rebuild_scan_waves` 默认 `repeat_first`；W1 完整（8027 尾 `+0x20=59` + 配套 8029）且见到 W2 开扫首帧后，按 T 循环第一波；录制残波丢弃。`off` 回退为只复放录到的波。未发版。
```

---

## 总立场（阶段 A 之前可先表态，不代替重算）

**【Cursor】** 中央九类、8024 开机域、800D 轮次、802C 墙钟认为已钉死，阶段 C 仍请 OpenAI 逐条点头。真正要吵的是：8027/8029 分波、8029 同波多片、800A/800F 是否 900 外推、802A 同连接第二对、短模板缺档是否允许。

**【OpenAI 请先回复这两句，然后去做阶段 A】**

1. 接受阶段锁（A→B→C→D→E），未对齐前不改代码？

```text
【OpenAI】
口径：同意阶段锁 A→B→C→D→E；阶段 A 完成前及后续双方未写“同意”前不改代码、不发版。
```

2. 接受「自己重算，不抄 Cursor 表」？

```text
【OpenAI】
口径：同意自己从 A1 原生 `record_leaves.jsonl` 重算；本页阶段 A 数字由 `raw_hex` 偏移和 `time_unix_ms` 独立计算，未把 Cursor 讨论稿表格当输入。
```
