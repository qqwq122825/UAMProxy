# DFMProxy v1.130.2 玩家设备池与用户批量删除说明

## 本次变更

1. 录制统一进入玩家池，不再创建或选择官方模板。
2. 录制会话自动保存到 `C:\PyProxyApp\v117_recording_pools.json`。
3. 重放先按 `model + system_version + IDFV` 严格匹配同设备录制。
4. 没有同设备候选时，按当前游戏ID回退到该账号的玩家录制。
5. 3366池始终绑定当前游戏ID，不跨账号复用。
6. 当前版本只读取统一玩家录制池，不读取旧版官方模板文件。
7. 用户管理表新增逐行选择框，支持勾选多个账号后一次确认删除。

## 验证

- `python3 -m unittest discover -s tests`
- 结果：245项通过，21项跳过。
