# DFMProxy 在线解密与重放稳定性修复：AI 交接报告

## 1. 任务目标

修改 DFMProxy 的 `01 0A 00 09` 处理流程，消除历史模板与当前 live 消息之间的
类别、批次、内部序号和时间错位，优先保证代理透明转发时连接稳定。

第一阶段原则：

```text
解密用于在线解析、分类、比较和记录
最终网络输出保持 live 原始字节
任一检查不确定时选择 PASS_LIVE
```

## 2. 已确认的故障

### 第一轮

```text
录制池：95
目标替换：84
最后 report：85
模板游标：84/95
三路解密 CRC：252/252
最终明文等于模板：84/84
完整语义签名匹配：14/84
历史时间落后：约312秒
```

report 84：

```text
live：956 B，10条叶子，seq 271～280
final：277 B，1条历史叶子，seq 262
```

report 85：

```text
live：982 B，6条叶子，seq 281～286
final：206 B，1条历史叶子，seq 263
```

### 第二轮

```text
录制池：95
目标替换：88
最后 report：89
模板游标：88/95
三路解密 CRC：264/264
最终明文等于模板：88/88
完整语义签名匹配：12/88
历史时间落后：约327秒
```

关键错位：

```text
report 85：template 11条 / live 1条
report 86：template 5条  / live 1条
report 87：template 1条  / live 11条
report 88：template 2条  / live 5条
report 89：两边seq均为284，但messageId不同
```

### 结论

```text
CRC和长度正确
样本池未耗尽
问题来自按全局第N条替换历史完整密文
任务调度、枚举顺序和批量聚合在不同运行中存在抖动
第N条模板与第N条live经常不是同一种业务消息
```

## 3. 当前协议与算法

### 外层目标记录

```text
01 0A 00 09
```

### 加密记录布局

```text
marker + 10字节前导
+ selector:u8
+ keyIndex:u8
+ plaintextCrc32:u32be
+ ciphertextLength:u16be
+ ciphertext
```

### selector

```text
0 = tersafe custom S-Box/XOR
1 = MARS
2 = RC6
```

已经闭环的 Python 实现：

```text
/Users/xxx/Documents/调试/tersafe模块分析/脚本/decode_type9_high_entropy.py
/Users/xxx/Documents/调试/tersafe模块分析/脚本/type9_crypto_constants.py
```

### 解密明文公共头

```text
+0x00 u32be version
+0x04 u16be declaredLength
+0x06 u32be recordCode
+0x0A u32be recordSequence
```

### 批量容器

```text
recordCode = 0x010A001B
+0x14 u8 childCount
+0x15 开始重复：u32be childLength + childRecord
```

### 二进制检测消息

```text
recordCode = 0x0102000A
+0x16 u16be messageId
```

## 4. 需要新增的模块

### 4.1 `Type9OnlineDecoder`

输入：完整 live 或 template 逻辑 payload。

输出：

```json
{
  "parse_ok": true,
  "selector": 2,
  "key_index": 3,
  "stored_plain_crc32": 0,
  "calculated_plain_crc32": 0,
  "plain_crc_ok": true,
  "top_record_code": 17432603,
  "child_count": 10,
  "leaves": [
    {
      "record_code": 16908298,
      "message_id": 32807,
      "record_sequence": 271,
      "declared_length": 134
    }
  ],
  "timestamps": [],
  "printable_strings": []
}
```

要求：

```text
解密失败只影响决策，不影响live转发
CRC计算使用 zlib/IEEE CRC32
所有整数明确大小端
批量记录递归解析
保存解析错误原因
```

### 4.2 `SemanticSignature`

```python
signature = (
    top_record_code,
    tuple((leaf.record_code, leaf.message_id) for leaf in leaves),
    child_count,
)
```

不要把 `recordSequence` 放进“消息类别签名”；序号单独比较。

### 4.3 `ConnectionSequenceState`

按连接代次保存：

```text
connection_id
connection_generation
last_outer_report_index
last_leaf_record_sequence
last_signature
current_scene
```

规则：

```text
新TCP连接清空状态
单条叶子通常为 previous + 1
批量叶子必须内部连续
重复、回退、跨越均记录原因
外层report和内部recordSequence分别维护，不要求数值相等
```

### 4.4 `ReplayDecisionGate`

第一阶段伪代码：

```python
def decide(live_bytes, template_bytes, state):
    live = decode(live_bytes)
    template = decode(template_bytes)

    if not live.parse_ok or not live.plain_crc_ok:
        return PASS_LIVE, "LIVE_PARSE_OR_CRC"

    if not template.parse_ok or not template.plain_crc_ok:
        return PASS_LIVE, "TEMPLATE_PARSE_OR_CRC"

    if live.signature != template.signature:
        return PASS_LIVE, "SIGNATURE_MISMATCH"

    if live.child_count != template.child_count:
        return PASS_LIVE, "BATCH_SHAPE_MISMATCH"

    if live.leaf_sequences != template.leaf_sequences:
        return PASS_LIVE, "SEQUENCE_MISMATCH"

    if contains_unmapped_dynamic_fields(live, template):
        return PASS_LIVE, "DYNAMIC_FIELDS_UNMAPPED"

    return PASS_LIVE, "OBSERVE_ONLY_MATCH"
```

当前阶段所有分支都输出 live，区别只体现在日志 reason。

## 5. 大批次保护

下面条件命中时直接 `PASS_LIVE` 并输出高优先级事件：

```text
child_count >= 2
live.child_count != template.child_count
max(live_length, template_length) / min(...) > 1.5
叶子包含 0xFFxx
叶子包含 0x100x
叶子包含 0x010x
叶子序号不连续
```

重点回归 report：

```text
第一轮：84、85
第二轮：85、86、87、88、89
```

## 6. 模板索引调整

移除“全部 type9 共用一个全局游标”的选择方式。

离线索引键：

```text
scene
+ topRecordCode
+ 有序 leaf(recordCode, messageId) 列表
+ childCount
+ structureVersion
```

模板索引第一阶段只用于分析命中率，不参与网络输出。

## 7. 日志格式

每次决策写 JSONL：

```json
{
  "time": "TIMESTAMP",
  "monotonic_ns": 0,
  "connection_id": "CONNECTION",
  "generation": 1,
  "report_index": 87,
  "decision": "PASS_LIVE",
  "reason": "BATCH_SHAPE_MISMATCH",
  "template_pool_index": 86,
  "template_length": 263,
  "live_length": 1083,
  "template_signature": [["0x0102000A", "0x8027"]],
  "live_signature": [
    ["0x0102000A", "0x8029"],
    ["0x0102000A", "0xFFFB"]
  ],
  "template_sequences": [281],
  "live_sequences": [268,269,270,271,272,273,274,275,276,277,278],
  "final_equals_live": true
}
```

## 8. 测试材料

第一轮：

```text
/Users/xxx/Documents/调试/三角洲/数据/01
```

第二轮：

```text
/Users/xxx/Documents/调试/三角洲/数据/02
```

现有分析器：

```text
/Users/xxx/Documents/调试/三角洲/分析_95录制_84处重放.py
/Users/xxx/Documents/调试/三角洲/分析_02录制重放.py
```

解密证据：

```text
/Users/xxx/Documents/调试/三角洲/95录制_84处重放分析_20260731
/Users/xxx/Documents/调试/三角洲/02录制重放分析_20260731
```

## 9. 必须通过的离线回归

```text
第一轮 report 84：PASS_LIVE / BATCH_SHAPE_MISMATCH
第一轮 report 85：PASS_LIVE / BATCH_SHAPE_MISMATCH
第二轮 report 85：PASS_LIVE / BATCH_SHAPE_MISMATCH
第二轮 report 86：PASS_LIVE / BATCH_SHAPE_MISMATCH
第二轮 report 87：PASS_LIVE / BATCH_SHAPE_MISMATCH
第二轮 report 88：PASS_LIVE / BATCH_SHAPE_MISMATCH
第二轮 report 89：PASS_LIVE / SIGNATURE_MISMATCH
```

全量断言：

```text
final_bytes == live_bytes
final_sha256 == live_sha256
不推进会改变后续输出的历史模板游标
解析异常不会DROP live
新连接不会继承旧连接sequence状态
```

## 10. 在线验收

```text
运行超过此前停止点
至少到 report 120
final == live 100%
外层CRC全部通过
明文CRC全部通过
内部叶子序号无代理引入的重复、回退或跳跃
任务大批次无代理引入的删除、提前或拆分
保存01双向、3366双向、FIN/RST和界面时间
```

## 11. 需要补充给修改者的源码

```text
DFMProxy工程根目录
REPLACE/PASS_NON_TARGET/DROP决策入口
模板池加载与游标推进模块
01帧切分与逻辑消息重组模块
final_output构造模块
CRC与重新分片模块
实际配置文件
构建和运行命令
```

推荐放置位置：

```text
/Users/xxx/Documents/调试/三角洲/DFMProxy-src
```

## 12. 交付要求

修改者应返回：

```text
源码变更
新增单元测试
01/02离线回归结果
决策日志样例
构建产物路径
透明在线测试记录
```
