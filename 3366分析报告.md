# 33 66 协议分析报告

> 分析日期：2026-03-18  
> 样本：第一次~第四次没开挂、第五次开挂录制  
> 核心发现：**01 0A 00 XX**（XX=09/21 等）为反作弊模块加密子包标记族

---

## 一、协议概述

`33 66` 是 ACE 反作弊框架的二进制帧协议，与 `01 00 00` 包共同运行在同一 SOCKS5 代理端口。

- **消息类型**：`10 01` 握手、`20 01` 认证质询、`40 13` 加密数据
- **加密方式**：AES-128-CBC，16 字节块对齐
- **数据流向**：客户端↔服务器，双向传输反作弊检测数据

---

## 二、核心发现：01 0A 00 09 与嵌套加密结构

### 2.1 重要结论

**40 13 明文包内部存在嵌套结构：明文包内还包含反作弊模块的加密子包。**

```
┌─────────────────────────────────────────────────────────────────┐
│  33 66 40 13 外层包（AES 解密后得到 plain_hex）                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  44 字节通用头部（MSG_SEQ、PROTO_LEN、CMD_TYPE、ABAB 等）      │ │
│  └─────────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  PAYLOAD（protobuf 等）                                       │ │
│  │  ┌─────────────────────────────────────────────────────────┐ │ │
│  │  │  01 0A 00 XX  [8 字节填充]  [序号]  [高熵加密区]            │ │ │
│  │  │  ↑ XX=09/21 等子类型             ↑ 反作弊模块加密后推送给游戏  │ │ │
│  │  └─────────────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**含义：**

- `01 0A 00 XX` 是**反作弊模块加密子包的前缀族**：第 4 字节 XX 为子类型（已知 09、21），**不是时间戳**，也不是明文业务字段
- 其后为 8 字节填充、1 字节序号，再后是**高熵加密区**（该段才是反作弊加密内容）
- 该加密区由反作弊模块本地加密后，作为子载荷嵌入 40 13 明文包，再经 33 66 通道推送给游戏服务器
- 即：**明文包内部仍有一层反作弊专用加密载荷**

### 2.2 结构解析（以 enc_len=192 为例）

```
ABAB 之后（偏移相对 ABAB 起始）：
  +0   00 00 00 01    — 块前缀（4B，固定）
  +4   00 84          — ★ LEN（2B 大端）= 从本字节前的 00000001 起到块尾总字节数
  +6   01 0A 00 09    — 反作弊加密子包标记（4B，第 4 字节为子类型 09/21 等）
  +10  00×8           — 8 字节填充
  +18  02 / 01        — 序号（1B）
  +19  01             — 内层类型（1B）
  +20  64 49 4E 5D …  — 高熵加密区（替换目标，从此到块尾）
```

**LEN 字段含义**（`00 00 00 01` 之后 2 字节大端）：

```
LEN = sizeof(00000001) + sizeof(LEN) + sizeof(01 0A 00 XX) + 8B填充 + 序号 + 类型 + 高熵区
    = 4 + 2 + 4 + 8 + 1 + 1 + 高熵区长度
```

| enc_len | raw_len | LEN (hex) | LEN (dec) | 子类型 |
|---------|---------|-----------|-----------|--------|
| 128     | 153     | 00 46     | 70        | 21     |
| 192     | 217     | 00 84     | 132       | 09     |
| 304     | 329     | 00 F6     | 246       | 09     |
| 592     | 617     | 02 19     | 537       | 09     |
| 928     | 953     | —         | —         | 09     |

**替换长度变化时必须同步更新**：若明文 `[4:8]`（PROTO_LEN）与块内 LEN 相等（单块包常见），替换高熵区后须**同时修改两处**：① 明文 `[4:8]` 的 4B PROTO_LEN；② `00000001` 后的 2B LEN。

**子类型变体**：除 `01 0A 00 09` 外，存在 `01 0A 00 21`（enc_len=128 等包）以及 `01 0A 00 13`、`01 0A 00 56` 等。第 4 字节为**子类型/版本**，非时间戳、非明文；替换逻辑一致：定位 `01 0A 00 XX`，跳过 8+1+1 字节后替换高熵区。

**enc_len=928 时**：结构相同，高熵加密区约 800+ 字节。

---

## 三、四轮没开挂录制中的 01 0A 00 09 包分布

| 录制       | 是否包含 01 0A 00 09 + 高熵加密区 | 行号        | enc_len 类型      |
|------------|-----------------------------------|-------------|-------------------|
| 第一次没开挂 | ✅ 有                              | 36, 46      | 192, 304          |
| 第二次没开挂 | ✅ 有                              | 47, 53, 59  | 192, 304, **928** |
| 第三次没开挂 | ✅ 有                              | 42, 47      | 192, 304          |
| 第四次没开挂 | ✅ 有                              | 46, 69      | 192, 304          |

**结论：**

- 四次没开挂录制中**均出现**含 `01 0A 00 09` 的高熵加密子包
- enc_len 主要为 192、304；第二次没开挂额外出现 enc_len=928 的长加密区包
- 说明该结构为**常规反作弊上报通道**，与是否开挂无关

---

## 四、40 13 明文通用头部（44 字节）

所有 40 13 明文包共享同一头部：

```
偏移      字节  含义
────────────────────────────────────────────────────────────────────
[0:4]      4   MSG_SEQ     — 外层 33 66 序号（大端）
[4:8]      4   PROTO_LEN   — ABAB 后载荷长度（大端）
[8:12]     4   CMD_TYPE    — 反作弊指令类型（大端）
[12:28]   16   ZEROS_16    — 全零保留
[28:32]    4   CTR_PLAIN   — byte[29] 为 40 13 包计数器
[32:36]    4   ZEROS_4     — 全零
[36:40]    4   TYPE_MARKER — 64 心跳: 00 08 00 00；其他: 00 00 00 00
[40:42]    2   CTR_ALL     — byte[41] 为全量消息计数
[42:44]    2   AB AB       — 帧结束魔数
[44:]      N   PAYLOAD     — 含 protobuf 及可能的 01 0A 00 09 子包
```

---

## 五、含 01 0A 00 09 的 enc_len 类型速查

| enc_len | 加密区约长 | 前缀变体   | 出现场景           | 重放策略     |
|---------|------------|------------|--------------------|--------------|
| 128     | ~54 字节   | 01 0A 00 21 | 反作弊子包（短），结构同 09 | 录制/替换策略后续定 |
| 192     | ~64 字节   | 01 0A 00 09 | 二进制指纹遥测     | 透传 + CTR 同步 |
| 304     | ~176 字节  | 01 0A 00 09 | 大型二进制指纹扫描 | 透传 + CTR 同步 |
| 928     | ~800 字节  | 01 0A 00 09 | 大型反作弊上报     | 透传 + CTR 同步 |

---

## 六、与 01 00 00 包的区别

| 维度       | 33 66 协议                    | 01 00 00 协议     |
|------------|-------------------------------|-------------------|
| 功能       | 账号验证 + 反作弊数据         | 反作弊数据采集    |
| 加密       | AES-128，16 字节对齐          | 自定义加密        |
| 01 0A 00 09 | **有**，明文包内含反作弊加密子包 | 无此结构          |
| 嵌套加密   | 外层 AES + 内层反作弊加密      | 单层加密          |

---

## 七、33 66 外层帧结构（16 字节头部）

```
偏移  长度  类型            说明
────────────────────────────────────────────────────────────────────
 0     2   uint8[2]       33 66 魔数（每帧固定开头）
 2     2   uint16 BE      协议版本（暗区为 00 0B）
 4     2   uint16 BE      子版本（暗区为 00 0B）
 6     2   uint8[2]       消息类型：10 01 / 10 02 / 20 01 / 40 13
 8     4   uint8[4]       Session ID ★ 全零=上行（客户端→服务器），非零=下行（服务器→客户端）
12     4   uint32 LE      序号（Sequence，小端）
16     N   Payload        类型相关载荷
```

**★ 上行 / 下行区分**：`frame[8:12]` 是 SessionId，**全 `00 00 00 00`** 时为上行，**非全零**时为下行（服务端在建立会话后写入 SessionId）。

**40 13 密文区定位**（total = 25 + enc_len）：

```
frame[16]   = 0x19（固定标记）
frame[19:21]  大端 uint16 = enc_len（密文区字节数，须为 16 的倍数）
frame[25:25+enc_len] = AES-128-CBC 密文（TAES 加密，见第七章 TAES 说明）
```

**10 01 握手**：含账号 ID 明文、游戏端口 0x2710、版本 0x66。载荷内 `02 03 00 00` + 2B 长度 + ASCII 账号；重放时账号需与录制一致。

**10 02 密钥下发**（服务端→客户端首帧）：`payload[4:7] == 10 02 10` 时，Key = `payload[7:23]`（16 字节，ASCII 可打印范围 `0x30..0x7A`）；IV 固定 `00 01 02…0F`。

```
10 02 示例（hex，完整帧）：
3366000B000B1002 01000000 01000000  3000000010021073 4B434968445548437
84D456875683801 …
│                │        │         │                 │─Key (16B)─────────
│                │        Seq LE    payload[0..6]
│                SessionId（非零=下行）
MsgType=10 02
```

**20 01 认证**：80 字节加密质询数据（部分实现兼容从 `pl[0:16]` 取 Key）。

**会话流程**：① `10 01` 上行握手 → ② `10 02` 下行密钥 → ③ `40 13` 双向加密数据持续传输。

---

## 七-B、TAES 加解密算法

### TAES 解密流程（单条 40 13）

```
1. 取密文：cipher = frame[25 : 25 + enc_len]
2. AES-128-CBC 解密：Key=sessionKey(16B)，IV=00 01 02…0F（固定）→ raw
3. 剥自定义填充（TAES padding）：
   - pad_len = raw[-1]，须 1 ≤ pad_len ≤ 32
   - 验证尾部魔数：raw[-6:-1] == b"tsf4g"（t=0x74,s=0x73,f=0x66,4=0x34,g=0x67）
   - 明文长度 = len(raw) - pad_len
4. 得到业务明文 plain（含 44 字节通用头 + PAYLOAD）
```

> 若 `tsf4g` 不存在但末尾 `pad_len` 个字节均等于 `pad_len`，则按 PKCS7 剥填充；两者都失败但明文含 `AB AB` 标志时也认为解密成功（代码容错逻辑）。

### TAES 加密（重放/替换时）

```
1. 计算 pad_len = 16 - (len(plain) % 16)，若为 0 则取 16；若 pad_len < 6 则 +16
2. 构造 tail = random(pad_len-6) + b"tsf4g" + bytes([pad_len])
3. padded = plain + tail
4. AES-128-CBC 加密：Key=sessionKey，IV=00 01 02…0F → cipher
5. 更新帧中 enc_len（frame[19:21] 大端）= len(cipher)
```

---

## 八、计数器与循环池（关键）

### 8.1 CTR_PLAIN（byte[29]）

40 13 全局包计数器，上行+下行共享。下行包不记录时会出现跳跃，但**上行方向单调递增**。重放时必须修补，否则池循环时计数器回退 = REPLAY 检测。

### 8.2 CTR_ALL（byte[41]）

33 66 连接级全量消息计数。同样需修补。

### 8.3 循环池修补

内存池循环复用时，**必须**强制 `CTR_PLAIN`、`CTR_ALL` 单调递增覆盖录制值，否则服务器检测到回退即判重放。

---

## 九、enc_len 速查与 CMD_TYPE

### 9.1 上行包 enc_len 速查

| enc_len | 用途 | 重放 |
|---------|------|------|
| 64 | 心跳/会话 ACK | 直接复用 |
| 80~144 | 遥测/状态快报 | 透传 + CTR 同步 |
| 128 | **含 01 0A 00 21 反作弊加密子包**（结构同 09，录制/替换后续定） | — |
| 192/304/928 | **含 01 0A 00 09 反作弊加密子包** | 透传 + CTR 同步 |
| 256~416 | 游戏状态/扫描结果 | 透传 + CTR 同步 |
| 448~464 | 角色/场景属性 | 透传 + CTR 同步 |
| 528/544 | 设备指纹（iPad/iPhone）| 544 需池替换（含 UA/UUID）|

### 9.2 CMD_TYPE 示例（enc_len=64 心跳）

`08 01 04 XX` 周期检测、`08 00 00 41` 基础检测、`0D 00 00 02/03` 状态上报、`08 F4 0B 43` 性能检测等。

---

## 十、替换方案（核心策略）

### 10.1 策略结论

**只需替换 `01 0A 00 09` 后面的高熵加密区即可。** 其他检测点（0B000002/0B000009 哈希、18 01 越狱、ERROR 内存扫描、r: 字段）经复盘为判断失误，无需处理。

### 10.2 替换目标与结构

```
ABAB 之后 PAYLOAD 结构（enc_len=192 为 01 0A 00 09，enc_len=128 为 01 0A 00 21）：
  00 00 00 01  00 84/00 46  …  01 0A 00 09/21  00 00 00 00 00 00 00 00  ??  01  [高熵加密区...]
  ↑ 前缀 4B    ↑ 长度           ↑ 子类型 4B      ↑ 8 字节填充              ↑ 序号 ↑ 类型 ↑ 替换区
```

- **保留**：44 字节头部、ABAB 及之前全部、`01 0A 00 XX`（XX=09/21）、8 字节填充、序号 1B、内层类型 1B（共 10 字节紧接 01 0A 00 XX 之后）
- **替换**：从 `01 0A 00 XX` 后第 10 字节起至子包结束的**高熵加密区**

### 10.3 替换流程

1. **解密**：对 40 13 密文做 AES 解密，得到 plain_hex
2. **定位**：在 plain_hex 中查找 `01 0A 00`，且下一字节为已知子类型（如 `09` 或 `21`），即匹配 `01 0A 00 09` 或 `01 0A 00 21`
3. **截取**：从 `01 0A 00 XX` 后第 10 字节起（跳过 8 字节填充 + 1 字节序号 + 1 字节类型），到子包结束，得到待替换的加密区
4. **替换**：从干净池按**加密区长度**取同长度数据，覆盖该区
5. **重加密**：用原密钥对修改后的 plain 重做 AES 加密
6. **修补**：按需更新 byte[0:4]、byte[29]、byte[41]（CTR 同步）
7. **写回**：写回 33 66 包体

### 10.4 干净池构建与加载策略

**加载时只加载携带 `01 0A 00 09` 或 `01 0A 00 21` 的包。** 当前录制为全量录制（含 enc_len=64、80、96 等），重放池应过滤为仅含 01 0A 00 XX（XX=09/21）的包，其余类型不纳入池。

从四次没开挂录制中提取，按加密区长度分桶：

| 加密区约长 | enc_len 对应 | 池来源 |
|------------|--------------|--------|
| ~54 B      | 128（01 0A 00 21） | 是否录制/入池后续定 |
| ~64 B      | 192          | 第一次 36 行、第二次 47 行、第三次 42 行、第四次 46 行 |
| ~176 B     | 304          | 第一次 46 行、第二次 53 行、第三次 47 行、第四次 69 行 |
| ~800 B     | 928          | 第二次 59 行 |

同一长度可轮换使用不同录制的干净数据，降低重复检测风险。

> **后续优化**：可改为仅录制含 01 0A 00 09 的包，减少录制体积与加载量。

### 10.5 优势

- **精准**：只动反作弊加密子包，其余 payload 与头部保持原样
- **简单**：无需解析 protobuf、无需哈希截断、无需多类型检测分支
- **可扩展**：新 enc_len 只需补充对应长度的干净数据到池

---

## 十一、下行帧结构补全（服务端 → 客户端）

### 11.1 下行帧类型总览

| 消息类型 | Hex    | 方向 | 说明 |
|----------|--------|------|------|
| 10 02    | MSG_SERVER_KEY | ↓下行 | **服务端首帧**，携带 AES-128 Key（payload[7:23]）；IV 固定 `00 01 02…0F` |
| 40 13    | MSG_DATA | ↓下行 | 加密数据帧，与上行共用相同加密结构，enc_len 可达 32 KB+（见下） |

### 11.2 10 02 密钥帧（Key/IV 下发）

- **首条下行帧**即为密钥帧，上行握手（10 01）后服务端立即回复
- 暗区突围固定格式：`payload[4:7] == 10 02 10`，Key 在 `payload[7:23]`，IV 固定 `00 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F`
- 代码提取路径：`try_extract_key_iv_from_first_downlink(frame, key_off, iv_off)`

### 11.3 下行 40 13 大包（社交/AC 数据）

服务端通过下行 40 13 帧推送账户社交数据 + 反作弊规则文件，解密后明文前 5 字节为**帧类型标志**：

| 明文前 5 字节     | 含义                              | 是否拦截 |
|-------------------|-----------------------------------|----------|
| `F8 02 00 00 03` | **AC 规则块下载帧**（ac_chunk）   | ★ 需拦截 |
| `F8 02 00 00 04` | 社交消息帧（好友列表 / 头像等）   | 通常放行 |
| 其他              | 常规账户/游戏状态数据              | 放行     |

> UI 代码注释原文：`F802000003=AC块，F802000004=社交消息不受影响`

下行大包（enc_len 可达 0x7E40 = 32320 字节）示例结构（解密后）：

```
偏移 0x0000  F8 02 00 00 03 …  — 帧类型前缀（5B，AC 规则块标志）
偏移 0x0005  protobuf 数据段   — 账号信息 / 好友 / 头像 URL 等社交数据
……
偏移 ~0x0A11 处（第2次命中）   — ★ 块拦截区间起始标记（0A 11，protobuf field tag）
……
偏移 ~末尾    50 4B 03 04 …    — ZIP 文件头（PK magic bytes）
              ├─ unzipmrpcs.data  — ZIP 条目 1（反作弊规则 protobuf）
              └─ （UT 后缀为 ZIP extra field 中的 Unix 时间戳字段）
```

**区间填充起始标记 `0A 11`（Hex）**：在 AC 规则块下载帧明文中，`0A 11` 是 protobuf 字段标识字节（field 1，wire type 2，length = 0x11 = 17），标志着某个子字段（含文件名或规则 ID）的开始。代码默认取**第 2 次命中**的 `0A 11` 作为清零区间的起始偏移，从此处到 `unzipmrpcs.data` 字符串前一字节整体填充 0x00，使反作弊模块无法正确解析规则文件。

### 11.4 反作弊文件下发机制（ZIP + unzipmrpcs.data）

ACE 通过下行 40 13 帧末尾附带 ZIP 格式反作弊规则文件：

- **触发标志**：明文前缀 `F8 02 00 00 03`（AC 块）
- **文件格式**：标准 ZIP（`50 4B 03 04` 开头），条目名 `unzipmrpcs.data`（`.dataUT` 为 ZIP extra field 中的 Unix 时间戳标记）
- **内容**：反作弊规则/配置（protobuf 编码），由客户端侧 ACE 模块解析并生效
- **ZIP 终止位置**：Central Directory Entry 结尾（可用字符串 `unzipmrpcs.data` 定位截止点）

### 11.5 下行拦截实现（dl_intercept.py）

下行拦截仅在**重放端口（1080）**生效，对每个到达的下行 40 13 帧执行：

```
接收下行 40 13 密文
  │
  ├─ 解密（AES-128-CBC，与上行同一 Key/IV）
  │
  ├─ 块下载拦截（ace_chunk_block_enabled=True）
  │     前缀匹配 F802000003 → 区间填充 [第N个 0A11, "unzipmrpcs.data" 前)
  │     效果：ZIP 数据区整体清零，阻止 ACE 更新规则
  │
  ├─ 字符串替换（dl_intercept_enabled=True）
  │     普通模式：查找 search_str → 等长 0x00 覆盖或替换为 replace_str
  │     毁掉模式：命中后按区间填充（同上区间配置，忽略 replace_str）
  │
  └─ 重加密（TAES 填充优先，fallback PKCS7）→ 发给客户端
```

**配置项**（config.json）：

| 配置键                          | 含义                                 | 默认/示例       |
|---------------------------------|--------------------------------------|-----------------|
| `dl_intercept_enabled`          | 是否启用下行拦截                     | false           |
| `dl_search_str`                 | 查找字符串（如 `rpcs-uam-ios-164`）  | —               |
| `dl_replace_str`                | 替换为（留空=等长 0x00 覆盖）        | —               |
| `dl_destroy_mode_enabled`       | 毁掉模式（区间填充，忽略替换串）     | false           |
| `ace_chunk_block_enabled`       | 3366 块下载拦截                      | false           |
| `ace_chunk_block_pattern`       | 块前缀 Hex（AC 块=`F802000003`）     | F802000003      |
| `ace_chunk_block_start_marker`  | 区间填充起始标记 Hex                 | 0A11            |
| `ace_chunk_block_start_marker_nth` | 取第 N 次命中                    | 2               |
| `ace_chunk_block_stop_marker`   | 区间填充终止字符串                   | unzipmrpcs.data |
| `ace_chunk_block_fill_byte`     | 填充字节（Hex）                      | 00              |

### 11.6 上行 config2/config3 拦截

代码中还存在**上行**拦截逻辑：若上行 40 13 明文含 `config2` 或 `config3` 字符串，则将该帧 enc_len 字段置 0（载荷清空），阻止反作弊相关配置上报。由 `ul_intercept_config23_enabled` 开关控制。

### 11.7 录制端口（1081）与重放端口（1080）

| 端口 | 模式 | 说明 |
|------|------|------|
| 1081 | `record`（录制） | 静默录制上行 01 包 + 上行 40 13 解密提取高熵区；不干预流量原样透传 |
| 1080 | `replay`（重放） | 接管上行流量，用录制池数据替换 01 包内 `0A 00 09` 区域 + 3366 的 `01 0A 00 09/21` 高熵区；同时执行下行拦截 |

录制端口只读不改包；替换仅在重放端口发生。两端口共用同一内存录制池（`recording_pool`），支持边录边播。

### 11.8 游戏账号 ID 识别

从上行 **01 包**中，找到 `0A 00 23` 标记后：`packet[78]` = ID 字节长度，`packet[79:79+len]` = ASCII 账号字符串（如 QQ 号）。  
从上行 **33 66 10 01 握手帧**中：载荷内 `02 03 00 00` + 2B 大端长度 + ASCII 账号字符串。  
两路账号应一致；若不一致，UI 会显示 `⚠双通道账号不一致` 警告。

---

## 十二、待验证项

1. **01 0A 00 09 子包解密**：内层加密算法与密钥（已知是反作弊模块本地加密，算法未逆向）
2. **子包序号语义**：`01 0A 00 09` 后 1B 序号（`01`/`02`/`03`…）的服务端校验规则
3. **928 包触发条件**：为何仅在第二次没开挂中出现，是否与特定反作弊检测事件相关
4. **下行 AC 块子类型 0x03 vs 0x04 精确区分**：`F8 02 00 00 03/04` 具体业务含义是否与代码注释完全一致
5. **unzipmrpcs.data 内容解析**：ZIP 内 protobuf 字段含义（反作弊规则结构、版本号、生效条件）
6. **`0A 11` 标记精确位置**：在不同版本/不同账户下是否固定为第 2 次命中，是否需要动态定位

其中越狱目录扫描包存在两种结果，示例如下：

**① ERROR 型（越狱目录访问失败）**

```
00000000  00 00 00 32 00 00 00 e7 08 10 00 03 00 00 00 00  |...2............|
00000010  00 00 00 00 00 00 00 00 00 00 00 00 00 30 00 00  |.............0..|
00000020  00 00 00 00 00 00 00 00 00 26 ab ab 00 00 00 01  |.........&......|
00000030  00 e7 01 12 23 36 00 00 00 00 00 0a 00 00 00 00  |....#6..........|
00000040  00 00 00 00 00 00 00 00 a4 00 01 12 56 00 00 00  |............V...|
00000050  00 00 00 00 00 00 00 00 01 00 00 09 4e 00 00 00  |............N...|
00000060  2d 6d 6f 64 65 6c 3a 69 50 61 64 31 33 2c 34 3b  |-model:iPad13,4;|
00000070  76 65 72 3a 31 34 2e 36 30 3b 69 6e 63 5f 69 64  |ver:14.60;inc_id|
00000080  3a 34 38 3b 6f 62 66 5f 69 64 3a 34 38 00 00 00  |:48;obf_id:48...|
00000090  00 04 00 00 00 0a 38 39 37 39 39 36 31 34 30 00  |......897996140.|
000000a0  00 00 00 01 00 00 00 00 06 45 52 52 4f 52 00 00  |.........ERROR..|
000000b0  00 00 06 45 52 52 4f 52 00 00 00 00 0a 00 00 00  |...ERROR........|
000000c0  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  |................|
000000d0  00 00 00 00 01 00 00 00 00 00 00 00 01 00 00 00  |................|
000000e0  00 00 00 00 00 00 00 00 00 8a a5 60 68 00 00 00  |...........`h...|
000000f0  00 8a a5 60 68 07 02 1e 00 a4 00 00 00 00 00 00  |...`h...........|
00000100  00 00 00 00 00 00 00 00 00 00 00 00 00 a4 00 00  |................|
00000110  00 01 01
```

结果字段出现 `ERROR`，表示该次越狱目录扫描**访问失败**（目录不存在或被拦截）。

---

**② 成功型（越狱目录访问成功，返回路径）**

```
--- Hex Dump (全部 273 bytes) ---
00000000  00 00 00 97 00 00 00 e5 08 10 00 03 00 00 00 00  |................|
00000010  00 00 00 00 00 00 00 00 00 00 00 00 00 7b 00 00  |.............{..|
00000020  00 00 00 00 00 00 00 00 00 77 ab ab 00 00 00 01  |.........w......|
00000030  00 e5 01 12 23 29 00 00 00 00 00 06 00 00 00 00  |....#)..........|
00000040  00 00 00 00 00 00 00 00 a4 00 01 12 56 00 00 00  |............V...|
00000050  00 00 00 00 00 00 00 00 01 00 00 09 4e 00 00 00  |............N...|
00000060  2d 6d 6f 64 65 6c 3a 69 50 61 64 31 33 2c 34 3b  |-model:iPad13,4;|
00000070  76 65 72 3a 31 34 2e 36 30 3b 69 6e 63 5f 69 64  |ver:14.60;inc_id|
00000080  3a 31 30 3b 6f 62 66 5f 69 64 3a 31 30 00 00 00  |:10;obf_id:10...|
00000090  00 04 00 00 00 0a 38 39 37 39 39 36 31 34 30 00  |......897996140.|
000000a0  00 00 00 01 00 00 00 00 01 00 00 00 00 19 2f 4c  |............../L|
000000b0  69 62 72 61 72 79 2f 4d 6f 62 69 6c 65 53 75 62  |ibrary/MobileSub|
000000c0  73 74 72 61 74 65 00 00 00 00 08 00 00 00 00 00  |strate..........|
000000d0  00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00  |................|
000000e0  00 00 01 00 00 00 00 00 00 00 01 00 00 00 00 00  |................|
000000f0  00 00 00 00 00 00 00 00 00 00 08 07 02 1e 00 a4  |................|
00000100  00 00 00 00 00 00 00 00 00 00 09 0b 91 66 52 01  |.............fR.|
```

结果字段出现 `/Library/MobileSubstrate`，表示该次越狱目录扫描**成功命中**，服务器将其判定为越狱设备。
