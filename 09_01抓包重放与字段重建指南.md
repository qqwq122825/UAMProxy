# 09. `01 00 00` 抓包重放与字段重建指南

## 1. 目标

本指南用于把一次干净采集中的 `type 9` 记录保存为模板，在后续抓包中：

1. 识别同类 `01 00 00` 逻辑包；
2. 保留当前连接和当前会话的外层字段；
3. 用干净模板替换目标记录；
4. 由内向外重算长度；
5. 重算完整逻辑 payload 的 CRC32；
6. 按 4096 字节重新分片并输出完整物理帧。

这里区分两个概念：

- **物理帧**：一次实际发送的 `01 00 00` 帧；
- **逻辑包**：一个或多个物理帧重组后的完整 payload。

CRC32 的输入是完整逻辑 payload。分片时，同一逻辑包的全部物理帧携带相同
CRC32。

## 2. 推荐的重放级别

### 2.1 原始整帧重放

直接重复以前抓到的全部字节。只适合验证客户端本地解析，因为帧序号、包组号、
传输标签、连接状态、报告序号和挑战数据可能已经变化。

### 2.2 当前外层 + 干净逻辑 payload

保留当前包的会话外层，替换完整 payload，然后重算 CRC、长度和分片。这适合
干净 payload 本身不含会话绑定字段的情况。

### 2.3 当前容器 + 干净目标记录

推荐模式。保留当前 type 9 容器、身份字段、报告序号和其他动态记录，只替换
`01 0A 00 09` 对应目标记录，再逐层重算长度和 CRC。静态与动态闭环已经确认
记录头后依次是10字节固定前导、算法选择器、静态密钥索引、明文 CRC32、
密文长度和密文。`04` 是 `key[4]`，不是算法类型。

采用“复制样本1密文”的方式时，下面这组字段必须作为一个不可拆分的完整单元
一起保存和替换：

```text
算法选择器 + 静态密钥索引 + 明文CRC32 + 密文长度 + 密文
```

只复制密文、同时保留样本2的选择器或密钥索引，会使解密算法、密钥和校验值
彼此不对应。

首版优先实现**等长记录替换**。干净记录和当前记录等长时，分片数量、分片边界
及多数字段保持稳定，只需替换 value 并重算 CRC。验证完成后再启用变长重建。

## 2.4 两种落地办法

### 办法一：长时间录制的顺序样本池

先在干净状态连续录制足够长的时间，例如1小时，按实际出现顺序保存数百条
`01 0A 00 09` 完整加密记录。实时运行时，样本2的第 `N` 条同类型报告使用
样本池中的第 `N` 条干净记录：

```text
干净采集：
clean[0], clean[1], clean[2], ... clean[N-1]

实时处理：
live[0]  → clean[0]
live[1]  → clean[1]
live[2]  → clean[2]
...
```

样本3始终保留样本2的：

```text
外层报告序号
帧序号
包组编号
传输标签
账号字段
当前外层容器
```

样本池提供一整套自洽的：

```text
算法选择器
静态密钥索引
明文CRC32
密文长度
密文
```

样本池应按消息类别分别建立游标，而不是把全部 type 9 记录混成一个队列：

```text
记录码/结构类型
+ 场景（大厅、对局、结算等）
+ 明文结构或密文长度类别
+ 出现顺序
```

若录制窗口覆盖1小时，而实际单次运行短于1小时，通常在本次运行中不会走到
样本池末尾，因此可以避免同一次运行内从第一条重新开始造成的明显计数器回退。
样本池用尽时应写入日志并标记：

```text
pool_exhausted = true
current_index
pool_size
message_class
```

随后可以重新采集更长的样本池。直接回绕到 `clean[0]` 会把密文内的
`inc_id/obf_id`、时间值及其他运行期数据一并带回旧值。

这一办法属于“长窗口原样密文模板”，它主要解决短时间内的数量覆盖和回绕问题。
历史绝对时间、连接状态或会话绑定字段仍然来自录制时的干净样本，因此需要通过
多次实际记录比较服务器是否检查这些字段的新鲜度。

推荐为每条模板保存：

```json
{
  "pool_index": 0,
  "capture_time": "TIMESTAMP",
  "scene": "lobby",
  "record_code": "010A0009",
  "outer_report_sequence_at_capture": 4,
  "algorithm_selector": 0,
  "key_index": 4,
  "plaintext_crc32": "FEB58046",
  "ciphertext_length": 313,
  "encrypted_record_hex": "0004FEB580460139...",
  "encrypted_record_sha256": "SHA256"
}
```

### 办法二：解密、字段合并、重新加密

后续高级模式不再循环历史完整密文，而是处理样本2的当前明文：

```text
解密样本2当前记录
→ 保留当前报告序号、inc_id/obf_id、时间、计数器和会话字段
→ 从干净样本库复制同类型检测结果字段
→ 重算明文CRC32
→ 使用样本2当前算法选择器和key重新加密
→ 重算内外层长度及外层CRC32
```

也可以在 `sub_D3720` 加密前截取当前明文，只合并目标字段，再让原函数完成
算法选择、密钥选择、CRC和加密。该方式天然继承当前运行期字段。

当前实施顺序：

```text
第一阶段：长时间录制顺序样本池
第二阶段：标定解密明文中的动态字段和检测结果字段
第三阶段：实现字段级合并与重新加密
```

## 3. 已确认的外层字段

### 3.1 首片

| 偏移 | 大小 | 处理规则 |
|---:|---:|---|
| `0x00` | 3 | 保持 `01 00 00` |
| `0x03` | 2 | 重算当前物理帧总长度，BE16 |
| `0x05` | 1 | 保留当前包 |
| `0x08` | 2 | 保留当前帧序号 |
| `0x0E` | 4 | 保留当前游戏/产品字段 |
| `0x24` | 2 | 保留或由当前发送队列分配包组号 |
| `0x26` | 2 | 写入重建后的分片总数，BE16 |
| `0x28` | 4 | 写入完整逻辑 payload CRC32，BE32 |
| `0x2C` | 1 | 首片写 `1` |
| `0x2D` | 2 | payload type，BE16 |
| `0x2F` | 1 | 使用当前实时包的传输标签字节 |
| `0x30` | 1 | 保留当前值，当前样本为 `0` |
| `0x31` | 2 | 首片编号，通常为 `1`，BE16 |
| `0x33` | 4 | 当前首片数据长度，BE32 |
| `0x37` | 变长 | 首片数据，最多 4096 字节 |

### 3.2 后续分片

| 偏移 | 大小 | 处理规则 |
|---:|---:|---|
| `0x03` | 2 | 重算当前物理帧总长度，BE16 |
| `0x24` | 2 | 与首片使用相同包组号 |
| `0x26` | 2 | 与首片使用相同分片总数 |
| `0x28` | 4 | 与首片使用相同完整 payload CRC32 |
| `0x2C` | 1 | 写 `0` |
| `0x2D` | 2 | 当前分片编号，BE16 |
| `0x2F` | 4 | 当前分片数据长度，BE32 |
| `0x33` | 变长 | 当前分片数据 |

`0x2F` 在首片和后片的含义不同：

```text
首片 0x2F：传输标签字节
后片 0x2F～0x32：当前分片数据长度
```

## 4. 传输标签与逻辑 channel

首片 `0x2F` 以前曾被标为 channel，当前静态和动态证据表明它是由全局状态
派生的传输标签字节：

```text
旧抓包样本：0x88
当前动态样本：0xA7
同账号重启样本A：0x70
同账号重启样本B：0x2B
```

重放时采用以下规则：

```text
传输标签字节 = 当前实时包的 0x2F
逻辑 channel  = 当前函数调用参数
```

例如 `sub_10CFE8` 的 `x3` 是实际逻辑 channel，动态记录中出现过 `2` 和
`10`。`sub_7BA4C` 的 `a4/x3=0或1` 更接近路由或序列化模式。这些参数均不应
用干净样本中的 `0x88/0xA7` 代替。

干净模板可以保存原始传输标签作为分析元数据，但生成新包时使用当前实时值。

## 5. 双重启样本确认的匹配边界

同一账号、同一游戏重启前后的两份样本各包含报告序号 `4、5、6`。稳定字段：

```text
游戏/产品ID候选 = 2382
账号身份字段     = 933579391
payload type     = 9
表A记录描述      = 01 0A 00 23
表B记录描述      = 01 0A 00 09
报告序号         = 4、5、6
```

变化字段：

```text
传输标签：0x70 → 0x2B
帧序号、包组编号、CRC32：变化
算法选择器：运行时可在0～2之间变化
静态密钥索引：运行时可在0～9之间变化
明文CRC32、密文长度和密文：随明文及算法结果变化
高熵主体长度和内容：变化
```

表 B 中 `01 0A 00 09` 后的当前结构：

```text
01 0A 00 09
00 00 00 00 00 00 00 00 00 00    10字节固定前导
SS                                算法选择器：0=自定义，1=MARS，2=RC6
KK                                静态密钥索引：0～9，例如04=key[4]
CC CC CC CC                       明文CRC32，BE32
LL LL                             密文长度，BE16
...                               LL LL指定长度的密文
```

旧报告里曾把 `SS KK` 合并显示为“未命名BE16”，现已更正。例如：

```text
00 04 = 算法0 + key[4]
00 08 = 算法0 + key[8]
00 09 = 算法0 + key[9]
01 04 = 算法1(MARS) + key[4]
```

因此模板匹配至少包含：

```text
账号身份字段
+ 报告序号
+ 表A记录码 0x0023
+ 表B记录码 0x0009
```

模板替换范围从算法选择器开始，一直到该记录密文结束。10字节固定前导可以重新生成。
采用密文模板模式时，`SS/KK/CRC32/密文长度/密文` 全部来自样本1；样本2只提供
当前会话的外层字段、身份字段、报告序号、传输标签及容器环境。

详细证据：

`网络分析/01同账号重启双样本对比报告.md`

## 6. 干净模板 JSON

推荐按“消息识别条件 + 可替换主体 + 来源元数据”保存：

```json
{
  "schema": "tersafe-type9-clean-record-v2",
  "name": "type9-report-0009-index4-clean",
  "match": {
    "outer_magic": "010000",
    "payload_type": 9,
    "account_identity": "933579391",
    "report_index": 4,
    "outer_record_descriptor": "010A0023",
    "inner_record_descriptor": "010A0009"
  },
  "replacement": {
    "mode": "opaque-encrypted-record",
    "fixed_prefix_hex": "00000000000000000000",
    "algorithm_selector": 0,
    "key_index": 4,
    "plaintext_crc32_be": "FEB58046",
    "ciphertext_length": 313,
    "ciphertext_hex": "CIPHERTEXT_HEX",
    "encrypted_record_hex": "0004FEB580460139CIPHERTEXT_HEX",
    "encrypted_record_sha256": "SHA256"
  },
  "source_metadata": {
    "capture_time": "TIMESTAMP",
    "frame_sha256": "SHA256",
    "logical_payload_sha256": "SHA256",
    "logical_payload_crc32": "4FDE7765",
    "transport_tag": "88",
    "algorithm_selector": 0,
    "key_index": 4,
    "frame_sequence": 35329,
    "packet_group": 7
  }
}
```

`source_metadata` 只用于审计和比较。下次生成包时，帧序号、包组号、传输标签和
CRC 均取当前状态或重新计算。

## 7. 干净样本采集

### 7.1 保存内容

每个干净样本建议同时保存：

```text
原始物理帧
重组后的完整逻辑 payload
type 9 解析结果 JSON
目标记录原始字节
目标记录 SHA256
表A/表B记录码
报告序号
10字节固定前导
算法选择器和静态密钥索引
明文CRC32、密文长度
完整加密记录（selector至密文末尾）及其SHA256
密文及其SHA256
能够离线解密时，再保存解密后的明文及其SHA256
完整 payload CRC32
帧序号、包组号和传输标签
采集时间、场景和账户状态标签
```

### 7.2 提取边界

不要按“找到 `01 0A 00 09` 后一直取到包尾”处理。该记录后可能还有其他
记录。高熵主体的结束位置应由所在记录、内层容器和外层容器的长度字段共同
确定。

解析顺序：

```text
type 9 payload
└── 表 A / 外层报告容器
    └── 表 B / 内层记录容器
        └── 目标记录头 01 0A 00 09
            ├── 10字节固定前导
            ├── 算法选择器
            ├── 静态密钥索引
            ├── 明文CRC32
            ├── 密文长度
            └── 密文
```

如果当前字段语义仍在确认，可先通过多份干净包做差分：

1. 同一场景连续采集至少三份；
2. 对齐 `01 0A 00 09`；
3. 比较前后长度字段；
4. 找出每个长度字段覆盖的确切范围；
5. 确认目标 value 后仍存在的尾部记录。

## 8. 重放算法

### 8.1 重组物理帧

按以下键聚合同一个逻辑包：

```text
包组号 + 分片总数 + 完整 payload CRC32
```

按 `fragment_number` 排序并拼接：

```python
logical_payload = b"".join(fragment.data for fragment in ordered_fragments)
```

验证：

```python
assert len(ordered_fragments) == fragment_count
assert crc32(logical_payload) == header_crc32
```

### 8.2 匹配目标消息

至少同时检查：

```text
payload_type == 9
账号身份字段符合模板
报告序号符合模板
表A描述为 01 0A 00 23
表B描述为 01 0A 00 09
目标记录结构长度有效
当前记录长度符合所选替换模式
```

只靠 `0A 00 09` 三字节搜索容易命中高熵区中的偶然字节序列，应使用完整
`01 0A 00 09` 并结合表 B 边界。

当前有两种模板选择模式：

```text
精确验证模式：账号 + 报告序号 + 表A/表B记录码
长窗口样本池：消息类别 + 场景 + 该类别出现次序
```

当前双样本可视化工具采用长窗口样本池模式，匹配键为：

```text
账号身份字段 + 表A记录描述 + 表B记录描述
```

报告序号不参与模板匹配，也不会从样本1复制到输出。工具按照样本1中同一匹配键
的出现顺序建立数组，并让样本2中同类消息的第 `N` 次出现使用 `clean[N]`；
游标到达数组末尾后会回绕并在页面提示。样本3外层报告序号始终保留样本2当前值。

离线解密已经观察到：

```text
外层报告4 → 明文 inc_id:10 / obf_id:10
外层报告5 → 明文 inc_id:11 / obf_id:11
外层报告6 → 明文 inc_id:12 / obf_id:12
```

精确模式利用报告序号避免这类交叉组合。长窗口模式依赖采集和实时运行的
同类消息顺序保持对齐，并通过足够长的样本数量避免本次运行内回绕；字段级
合并模式则会在明文层保留实时 `inc_id/obf_id`。

### 8.3 替换目标记录

从表 B 起点按结构解析，不使用固定绝对偏移：

```python
inner_length_offset = 24 + identity_length
inner_start = inner_length_offset + 2
descriptor_start = inner_start + 6
fixed_prefix_start = descriptor_start + 4
algorithm_selector_offset = fixed_prefix_start + 10
key_index_offset = algorithm_selector_offset + 1
plaintext_crc32_offset = key_index_offset + 1
ciphertext_length_offset = plaintext_crc32_offset + 4
ciphertext_start = ciphertext_length_offset + 2
ciphertext_end = ciphertext_start + be16(payload, ciphertext_length_offset)
```

密文模板模式下，先从样本1提取一个自洽的加密记录：

```python
clean_encrypted_record = (
    bytes([clean_algorithm_selector])
    + bytes([clean_key_index])
    + clean_plaintext_crc32.to_bytes(4, "big")
    + len(clean_ciphertext).to_bytes(2, "big")
    + clean_ciphertext
)
```

等长模式：

```python
assert len(clean_encrypted_record) == current_encrypted_record_length
payload[algorithm_selector_offset:current_ciphertext_end] = clean_encrypted_record
```

变长模式：

```python
payload = (
    payload[:algorithm_selector_offset]
    + clean_encrypted_record
    + payload[current_ciphertext_end:]
)
delta = len(clean_encrypted_record) - current_encrypted_record_length
```

然后从最内层向最外层更新长度：

```text
密文长度
→ 表 B 容器长度
→ 表 B 回显长度
→ 表 A 回显长度
→ 完整逻辑 payload 长度
```

`0x0023` 和 `0x0009` 是记录码/结构 ID，不随长度增减。

每一级长度字段的定义应按“是否包含自身头部”分别记录，避免统一加 `delta`。

### 8.4 重算 CRC32

```python
import zlib

crc32_value = zlib.crc32(bytes(logical_payload)) & 0xFFFFFFFF
crc_be = crc32_value.to_bytes(4, "big")
```

写入每个物理分片的 `0x28～0x2B`。

旧 CRC 可以保存在日志中：

```text
原始 CRC
修改后 CRC
运行时重新解析后的 CRC
三者对应的 payload SHA256
```

### 8.5 重新分片

```python
chunks = [
    logical_payload[pos:pos + 4096]
    for pos in range(0, len(logical_payload), 4096)
]
fragment_count = len(chunks)
```

首片数据头长度为 `0x37`，后片数据头长度为 `0x33`：

```python
first_frame_length = 0x37 + len(chunks[0])
later_frame_length = 0x33 + len(chunk)
```

每个物理帧还要写入：

```text
整帧长度
分片总数
分片编号
当前分片数据长度
完整逻辑 payload CRC32
```

## 9. 当前 390 字节样本示例

当前示例：

```text
整帧长度：0x0186 = 390
首片标记：0x01
payload type：0x0009
传输标签：0x88
保留字节：0x00
分片编号：0x0001
分片数据长度：0x0000014F = 335
存储 CRC32：0x4FDE7765
```

长度关系：

```text
0x37 + 0x14F = 0x186
55 + 335 = 390
```

若替换后的目标 value 与原 value 等长：

```text
整帧长度仍为 0x0186
分片数据长度仍为 0x14F
传输标签继续使用当前实时包的值
CRC32 对新的 335 字节 payload 重新计算
```

若替换后 payload 增加 `N` 字节且仍小于等于 4096：

```text
payload长度 = 0x14F + N
整帧长度    = 0x186 + N
CRC32       = CRC32(修改后的完整payload)
```

超过 4096 后按第 8.5 节重新分片。

## 10. 新鲜度和会话字段

干净记录中可能包含：

- 时间戳；
- 报告序号；
- 服务器挑战值；
- 连接或设备标识；
- 单调计数器；
- 基于会话材料生成的摘要或 MAC。

因此按以下优先级选择替换范围：

```text
仅替换固定检测结果字段
优于
替换完整目标记录
优于
替换完整逻辑 payload
优于
重复完整历史物理帧
```

同一干净记录在跨重启使用前，应比较至少两次会话中的差异：

```text
固定字节区：可进入模板
随会话变化区：保留当前值
 算法选择器/密钥索引/明文CRC32/密文长度：与对应密文一起取自同一模板
递增或时间相关区：从当前包复制
校验/摘要区：按对应算法重建
```

## 11. 抓包重放时机

推荐先收齐一个完整逻辑包，再修改并输出：

```text
捕获首片
→ 暂存包组状态
→ 收齐全部分片
→ 重组、替换、重算
→ 一次性输出重建后的全部分片
```

避免在首片到达时立即发送，因为此时完整 payload 和最终 CRC 的输入可能还没有
收齐。

单片包可以直接进入“解析→替换→重算→输出”流程。

## 12. 运行日志

每次重放至少记录：

```json
{
  "timestamp": "TIMESTAMP",
  "mode": "equal-length-body",
  "payload_type": 9,
  "account_identity": "933579391",
  "report_index": 4,
  "outer_record_code": "0023",
  "inner_record_code": "0009",
  "algorithm_selector_clean": 0,
  "key_index_clean": 4,
  "plaintext_crc32_clean": "FEB58046",
  "packet_group": 7,
  "fragment_count_before": 1,
  "fragment_count_after": 1,
  "transport_tag_live": "A7",
  "clean_template": "type9-report-0009-index4-clean",
  "old_payload_length": 335,
  "new_payload_length": 335,
  "old_crc32": "CRC32",
  "new_crc32": "CRC32",
  "old_payload_sha256": "SHA256",
  "new_payload_sha256": "SHA256"
}
```

日志应同时保存修改前后 payload，方便离线复查长度和 CRC。

## 13. 验证清单

发送前逐项检查：

```text
[ ] magic 为 01 00 00
[ ] 每个物理帧的声明长度等于实际长度
[ ] 分片编号连续，从 1 开始
[ ] 分片总数一致
[ ] 每片声明的数据长度等于实际数据长度
[ ] 拼接后的 payload 长度正确
[ ] 每片携带相同完整 payload CRC32
[ ] CRC32 与重组后的 payload 匹配
[ ] type 9 内层全部长度字段边界有效
[ ] 表A/表B记录码仍为0023/0009
[ ] 报告序号与模板匹配
[ ] 算法选择器、密钥索引、明文CRC32、密文长度与密文来自同一份模板
[ ] 当前帧序号、包组号和传输标签得到保留
[ ] 当前动态字段没有被旧会话数据整体覆盖
```

离线验证命令：

```bash
python3 "/Users/xxx/Documents/调试/tersafe模块分析/脚本/analyze_01_packets.py" \
  REPLAY_PACKET.bin \
  --json REPLAY_PACKET.analysis.json \
  --extract-dir REPLAY_PACKET.frames
```

重点确认：

```text
payload_length_matches = true
payload_crc32_matches  = true
fragment_count         = 实际分片数量
fragment_number        = 连续编号
```

## 14. 推荐实施顺序

1. 先实现单片、type 9、同报告序号的等长主体替换；
2. 加入 CRC32 自动重算；
3. 加入修改前后 JSON 日志；
4. 加入多分片重组与重新分片；
5. 标定表 A、表 B 和高熵主体的精确边界；
6. 开启变长替换；
7. 最后加入多模板匹配和跨会话差分。

现有离线解析入口：

```text
工具/01心跳包离线解密.html
工具/01包字段可视化分析器.html
脚本/analyze_01_packets.py
脚本/analyze_dynamic_trace.py
```

## 15. 双样本生成样本3的实际示例

假设：

```text
样本1 = 01 原始数据包上传.md      （干净模板）
样本2 = 01 原始数据包上传2.md     （当前待发送包）
样本3 = 替换与重算后的输出
```

匹配到三个报告：

| 报告序号 | 样本1完整加密记录 | 样本2完整加密记录 | 样本3来源 |
|---:|---:|---:|---|
| `4` | `321B` | `231B` | 样本1的 `321B` |
| `5` | `397B` | `945B` | 样本1的 `397B` |
| `6` | `837B` | `327B` | 样本1的 `837B` |

样本3保留样本2的当前会话字段：

```text
传输标签：0x2B
帧序号：25546、25547、25548
包组编号：6、7、8
账号身份：933579391
报告序号：4、5、6
表A/表B记录描述：010A0023 / 010A0009
算法选择器/密钥索引：0/4、0/9、0/4（完整加密记录来自样本1）
```

算法重新生成：

| 报告 | 样本2帧长 | 样本3帧长 | 样本2 payload | 样本3 payload | 样本3 CRC32 |
|---:|---:|---:|---:|---:|---:|
| `4` | `342` | `432` | `287` | `377` | `0x7279A388` |
| `5` | `1056` | `508` | `1001` | `453` | `0xE49C0CA4` |
| `6` | `438` | `948` | `383` | `893` | `0x2AF9A574` |

样本3：

```text
总长度：1888字节
物理帧：3
SHA256：51a7cea4adf675249d8244950b7f05aceeee94c674065ade28b367c94f163eb0
三个CRC32：全部离线复算匹配
```

生成文件：

```text
网络分析/01样本3_示例重建.bin
网络分析/01样本3_示例重建.hex.txt
网络分析/01样本3_示例重建.analysis.json
```

可视化工具：

`工具/01双样本替换重建可视化.html`

页面中：

```text
蓝色下划线 = 样本2当前会话字段
绿色下划线 = 样本1完整加密记录
黄色下划线 = 长度与CRC等重算字段
```
