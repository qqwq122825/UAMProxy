# 01 简约替换指南

## 一句话规则

**以完整干净模板为主体：蓝色字段继承实时包，黄色 CRC 重新计算，其余绿色字段原样保留。**

## 字节来源

| 颜色 | 字段 | 来源/处理 |
|---|---|---|
| 蓝色 | `0x06..0x09` | 继承实时会话/帧序号 |
| 蓝色 | `0x12..0x25` | 继承实时会话字段和包组次数 |
| 蓝色 | `0x2F` | 继承实时传输标签 |
| 蓝色 | `01 0A 00 23` 后 4 字节 | 继承实时报告次数，BE32 |
| 黄色 | `0x28..0x2B` | 覆盖报告次数后重算外层 CRC32 |
| 绿色 | 其他全部字段 | 保留干净模板 |

绿色字段包括：

```text
物理帧长度和分片结构
0x05、0x0A..0x11 等固定头字段
游戏 ID（必须与实时连接匹配）
01 0A 00 09 完整加密记录
算法选择器、密钥索引、明文 CRC 和密文
```

## 最简流程

```text
识别实时游戏 ID
→ 选择同游戏 ID 的干净模板池
→ 按游标循环取完整模板
→ 覆盖蓝色字段
→ 重算黄色外层 CRC32
→ 校验长度、分片、CRC、游戏 ID
→ 发送
```

伪代码：

```python
out = copy(clean_template)
out[0x06:0x0A] = live[0x06:0x0A]
out[0x12:0x26] = live[0x12:0x26]
out[0x2F] = live[0x2F]

clean_payload[clean_report_offset:clean_report_offset + 4] = (
    live_payload[live_report_offset:live_report_offset + 4]
)
crc = crc32(clean_payload)
write_crc_to_all_fragments(out, crc)
```

## 关键说明

- 报告次数替换是等长 4 字节覆盖，所以模板总长度和分片结构保持不变。
- 只重算外层完整逻辑 payload CRC；模板中的明文 CRC 和加密记录保持干净值。
- 若实时报告次数与模板相同，重算后的外层 CRC 数值也可能与模板相同。
- 多分片模板的每个分片都写入同一个新 CRC，帧序号从实时首片开始递增。
- 新录制收到 42B 加入包时清空旧 01 池；不同游戏 ID 的池始终隔离。
- 任一最终校验失败时丢弃本帧组并记录原因。

## 最终校验

```text
声明物理长度 == 实际长度
分片编号连续且分片总数一致
每片数据长度正确
所有分片 CRC 一致
crc32(重组后的完整 payload) == 输出 CRC
输出游戏 ID == 当前连接游戏 ID
```
