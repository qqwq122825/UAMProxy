AI 开发与协作准则

目的：规范人与 AI 的协作流程，减少误操作，保证变更可追溯与可复现。

1. 请求确认（强制）
- 每次用户提出开发或修改请求时，AI 必须**用自己的理解将用户的原始需求复述一遍以确认**（例：用户原话 → AI 的理解），以防止因字面或语义差异导致的误解。
- AI 在复述后等待用户显式回复“是的，按此执行”或提供补充说明，才进行任何代码/文档修改。

2. 澄清提问（保守原则）
- 若需求不完整或存在歧义，AI 最多提出 3 个关键澄清问题并等待回答，不能擅自做假设。

3. 变更提交流程
- 所有代码修改以补丁（diff）形式应用（在当前工作区使用 `apply_patch`），并在修改前向用户展示补丁摘要以供确认。
- 修改后运行相关测试或静态检查（若项目包含测试/检查工具），并把执行结果返回给用户。

4. 提交说明
- 每次变更提供简短的 1-3 行变更说明：为什么改、改了什么、是否有兼容性影响。
- 推荐提供一个样例 `git commit` 信息供用户直接使用。

5. 安全与敏感操作
- 涉及安全、凭证、加密或认证的修改必须先获得人工审批（显式 “同意/拒绝”）。

6. 自动化与授权
- 若用户主动授予权限，AI 可在获得确认后直接创建/修改文件并运行测试；默认情况下，AI 在执行修改前应等待用户最终确认。

7. 可追溯性
- 保留与代码变更相关的对话、确认记录与补丁，以便审计与回溯。

8. 放置建议
- 将本文件命名为 `AI_DEV_GUIDELINES.md` 并放置在仓库根目录，便于所有协作者查看。
- 在 `README.md` 顶部或贡献指南处添加一行指向本准则的链接。

9. 示例交互流程
1) 用户：提出需求或贴出要修改的原话。
2) AI：逐字复述用户原话，并列出需确认的实现细节（若有）。等待用户确认/补充。
3) 用户：确认或补充说明。
4) AI：展示拟执行的补丁摘要，等待最终确认。
5) 用户：确认后 AI 执行补丁、运行测试并汇报结果。

---

（说明：如需我现在同时在 `README.md` 添加一个指向本文件的链接，请回复“是，添加链接”或“否”。）