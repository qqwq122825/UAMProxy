# 33 66 协议 01 0A 00 09 前置长度字段分析

> 分析日期：2026-03-19  
> 主题：`01 0A 00 09` 前长度字段（02 19 / 00 84 / 00 F6 等）包含什么

---

## 一、结构概览

在 40 13 明文包的 **ABAB 之后**，当存在反作弊加密子包时，PAYLOAD 呈现如下结构：

```
ABAB 之后：
┌──────────────────────────────────────────────────────────────────────────┐
│  00 00 00 01  │  LEN (2B)  │  01 0A 00 XX  │  8B 填充  │  序号  │  类型  │  高熵加密区  │
│  4 字节前缀    │  长度字段   │  子类型 4B    │           │  1B    │  1B    │  (替换目标)  │
└──────────────────────────────────────────────────────────────────────────┘
       ↑              ↑              ↑
    块起始        本文重点        反作弊子包标记
```

**长度字段 LEN** 为 2 字节、**大端序**，紧跟在 `00000001` 之后、`01 0A 00 XX` 之前。

---

## 二、长度字段包含什么

### 2.1 结论

**LEN 表示：从 `00000001` 开始（含 `00000001` 和 LEN 自身）到该逻辑块结束的总字节数。**

即：

```
LEN = 4 + 2 + (01 0A 00 XX 及之后到块尾的字节数)
    = 00000001(4) + LEN(2) + 01 0A 00 XX(4) + 8B 填充 + 序号 + 类型 + 高熵加密区
```

### 2.2 实测验证

以 enc_len=592、raw_len=617 为例（用户提供样本）：

| 字段 | 十六进制 | 十进制 | 说明 |
|------|----------|--------|------|
| 00000001 | 00 00 00 01 | — | 块类型/前缀 |
| LEN | 02 19 | 537 | 块总长度 |
| 01 0A 00 09 | 01 0A 00 09 | — | 子类型 09 |

**实际长度验证**：
- 从 `00000001` 到明文结尾：**537 字节** ✓
- 与 LEN 值 537 完全一致

从 `01`（01 0A 00 09 的首字节）到结尾：**531 字节** = 537 - 6（00000001 4B + LEN 2B）

---

## 三、不同 enc_len 下的 LEN 取值

| enc_len | raw_len | LEN (hex) | LEN (十进制) | 子类型 XX |
|---------|---------|-----------|--------------|-----------|
| 192 | 217 | 00 84 | 132 | 09 |
| 304 | 329 | 00 F6 | 246 | 09 |
| 592 | 617 | 02 19 | 537 | 09 |
| 128 | 153 | 00 46 | 70 | 21 |
| 96 | 121 | 00 22 | 34 | 13 |
| 80 | 105 | 00 15 | 21 | 56 |

规律：
- LEN 随子包（含高熵加密区）变大而变大
- 同一 enc_len 下，LEN 与头部 `[4:8]` 的 PROTO_LEN 常一致（如 00000219、00000084）

---

## 四、与包头部 PROTO_LEN 的关系

40 13 明文头部 `[4:8]` 为 4 字节大端 **PROTO_LEN**，表示 ABAB 后 PAYLOAD 总长度。

当 PAYLOAD 中**仅有一个** `00000001 + LEN + 01 0A 00 XX` 块且占满整个 PAYLOAD 时：

```
PROTO_LEN (4B) ≈ LEN (2B)
```

例如：
- 头部 `[4:8]`：`00 00 01 30` = 304
- 块内 LEN（`00000001` 后）：`01 30` = 304
两者相同，说明该包只有这一个逻辑块，且 LEN 与 PROTO_LEN 一致。

> **⚠️ 核心替换规则（2026-03-21 修正）**：
> 如果在原始明文中，包头的 **PROTO_LEN** 和块内的 **LEN** 长度一致（如都是 `01 30` 或 `02 19` 等），那么在替换高熵区导致长度变化时，**必须同时修改这两个位置的长度字段**！
> - 修改点 1：`[4:8]` 的 4 字节大端 PROTO_LEN
> - 修改点 2：`00000001` 之后的 2 字节大端 LEN
> 
> 如果 PROTO_LEN > LEN，说明载荷内还有其他协议数据，通常只需修改块内 LEN，并让外部封装做适应。但在实际处理单一块的 3366 反作弊包时，两者往往一致并需要同步更新。

---

## 五、字节级布局示意

以 enc_len=592 为例（01 到结尾 531 字节）：

```
偏移(相对 ABAB)  内容
────────────────────────────────────────────────────────────
0-3              00 00 00 01    块前缀
4-5              02 19          长度 = 537（含本块从前缀到块尾）
6-9              01 0A 00 09    反作弊子包标记
10-17            00×8           8 字节填充
18               01/02/03       序号
19               01             内层类型
20-531           高熵加密区      待替换的目标区
```

---

## 六、解析与替换时的用法

1. **定位块**：在 ABAB 后查找 `00 00 00 01`
2. **读长度**：取紧随其后的 2 字节大端整数 LEN
3. **校验**：从 `00000001` 起取 LEN 字节，应包含完整的 `01 0A 00 XX` 及高熵区
4. **替换区**：从 `01 0A 00 XX` 后第 10 字节起（跳过 8B 填充 + 序号 + 类型）到块尾，即高熵加密区

---

## 七、小结

| 项目 | 说明 |
|------|------|
| **长度字段位置** | `00000001` 之后 2 字节 |
| **字节序** | 大端 (Big-Endian) |
| **含义** | 从 `00000001` 起（含自身和 LEN）到块尾的字节数 |
| **与 PROTO_LEN** | 单块包中数值相同；多块包中 LEN 为当前块长度 |
| **01 到结尾** | LEN - 6 字节（去掉 00000001 4B + LEN 2B） |
