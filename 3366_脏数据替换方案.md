# 33 66 协议「脏数据」替换方案

> 本文与下列文档一致，请交叉阅读：  
> - `3366_密钥与解密说明.md` — 会话 Key、IV、`aes_cbc_4013`、40 13 密文截取  
> - `3366协议01_0A_00_09长度字段分析.md` — `PROTO_LEN`、块内 `LEN`、单块/多块关系  
> - `3366分析报告.md` — 40 13 通用头、TAES、CTR、`01 0A 00 XX` 子包、替换流程 §10  

**「脏数据」**：可能触发风控或泄露环境的路径/指纹，例如明文里的 `TweakInject`、`*.dylib` 路径、`auto_defence_start` 等来源标识符，或 `01 0A 00 09` 子包内的高熵加密区（与干净设备录制对比）。  
**「干净数据」**：来源标识符为正常格式（`cs:hash` 或空）且已用「干净池」同长度替换后的内层密文。

---

## 一、两类脏数据形态（策略不同）

| 类型 | 特征 | 推荐策略 |
|------|------|----------|
| **A. 明文设备字符串** | 在 AB AB 后的可读 ASCII 区，`08100003` 类上报包含一个设备字符串 TLV，其**来源标识符**（`;model:` 之前的字段）为 `/usr/lib/...dylib`、`auto_defence_start` 等敏感内容。 | **仅替换来源标识符**（`;model:` 之前的部分），保留 `;model:xxx;ver:xxx;inc_id:xxx;obf_id:xxx` 不变；见 §一-A。 |
| **B. `01 0A 00 09` / `21` 子包** | 在 PAYLOAD 中可定位 `01 0A 00 XX`，其后 8B 填充 + 序号 + 类型 + **高熵加密区**。 | **同长度替换**高熵区（干净池），**不改变**明文总长度时 enc_len 往往不变（仍可能因 TAES 填充边界变化 16 字节，见 §二）。 |

两类可并存：同一 40 13 明文里既有 A 类明文，又有 B 类子包时，按区域分别处理后再统一 TAES 加密。

---

## 一-A. 设备字符串字段结构（A 类详解）

### 字段格式

```
[1B len] [来源标识符] ; model:xxx ; ver:xxx ; inc_id:xxx ; obf_id:xxx
```

- `len`：后续整个字符串的字节长度（包含来源标识符和 `;model:...` 全部内容）。  
- 服务端按 `;` 分割后依次解析 `model:`、`ver:`、`inc_id:`、`obf_id:` 字段；**来源标识符**是第一个字段（分号之前），用于判断运行环境。

### 已知来源标识符类型（实测样本）

| 来源标识符 | 示例 | 类型 | 服务端含义 |
|-----------|------|------|-----------|
| `cs:hash1/hash2` | `cs:b1aee09a/8e672736` | **正常** | 游戏二进制校验和，干净设备上报 |
| *(空，字符串直接以 `model:` 开头)* | `-model:iPad13,4;...` | **正常** | 无标识符，极简上报 |
| `iTeamID:xxx` | `iTeamID:6H9S4KQ3C9` | 开发版 | Apple 企业/团队签名包 |
| `/usr/lib/XXX.dylib` | `/usr/lib/TweakInject/eacaq.dylib` | **脏** | 越狱插件注入路径，触发风控 |
| `auto_defence_start` | `auto_defence_start` | **脏** | 防检测工具注入标记，触发风控 |

### 替换原则

> **关键限制：不能全量置零。**  
> 服务端必须从字符串中读取 `model:`、`ver:`、`inc_id:`、`obf_id:` 字段。若将整个字符串（含 `;model:...`）全部清零，服务端无法找到这些键 → 解析失败 → 可能断连。

**仅替换来源标识符部分**（`;model:` 之前），策略如下：

| 策略 | 操作 | 是否需改长度字段 |
|------|------|----------------|
| **等长置零** | 将来源标识符部分（`/usr/lib/...` 或 `auto_defence_start`）原地替换为同等长度的 `\x00` | **否**，`len` 字节、PROTO_LEN、LEN 均不变 |
| **等长伪装（推荐）** | 替换为 `cs:00000000/00000000` 等同长度合法格式（补空格/零对齐） | **否**，同上 |
| **变长替换** | 替换为更短的 `cs:hash` 或直接删除来源标识符 | **是**，需同步更新 `len` 字节、PROTO_LEN、块内 LEN（见 §三） |

**第二处路径字段**（长度前缀 `21`=33 字节，纯路径无 `;model:`）：可直接等长全零，不影响解析。

---

## 二、替换后密文长度与 `enc_len`（TAES 层）

实现参考：`core/protocol_3366.py` 中 `_taes_encrypt`、`try_decrypt_4013_frame` 及 40 13 帧布局（`3366分析报告.md` §七-B）。

### 2.1 关系链

```
明文 plain（业务逻辑长度）
  → TAES 填充（tail 含 tsf4g + pad_len）
  → padded 长度 = 16 的倍数
  → AES-128-CBC 密文 cipher
  → len(cipher) = enc_len（须写入 frame[19:21] 大端）
```

帧总长：`total = 25 + enc_len`（`3366分析报告.md` §七）。

### 2.2 长度变化何时发生

- **明文长度变化**（删字符串、改 TLV）：`len(plain)` 变化 → `padded` 可能跨 **16 字节块边界** → 新的 `len(cipher)` **可能比原值多或少 16 的倍数**（通常 0 或 ±16，极端情况下 ±32…）。  
- **仅 B 类同长度替换**：`len(plain)` 不变 → 多数情况下 `enc_len` **不变**；若仍因实现细节（如 `pad_len < 6` 时 +16 的规则）导致填充变化，需以**重算后的 `len(cipher)`** 为准。

### 2.3 写回帧时必须改动的字段

1. **`frame[19:21]`**：大端 `uint16`，`enc_len = len(new_cipher)`，必须为 **16 的倍数**且 **> 0**。  
2. `frame[25:]`：替换为**新密文**（长度 = `enc_len`），旧帧尾部多出的字节删除、不足则补齐（整帧替换为 `25 + enc_len` 字节）。  
3. 代理/日志中展示的 **CIPHER=xxxB** 与 **PLAIN=xxxB** 会随上述变化。

---

## 三、明文长度变化时：长度字段联动（`PROTO_LEN` / `LEN`）

依据 `3366协议01_0A_00_09长度字段分析.md` §四、§七。

### 3.1 40 13 头 `[4:8]` — `PROTO_LEN`

- **含义**：AB AB **之后** PAYLOAD 的总字节数（大端）。  
- **A 类**修改了 AB AB 之后的任意长度（增删字符串、改 TLV）：必须重算 **从 `AB AB` 后第一个字节到 PAYLOAD 末尾** 的长度，写入 **4 字节大端 PROTO_LEN**。  
- **若 PAYLOAD 仅含一个 `00000001 + LEN + 01 0A 00 XX` 块** 且单块占满 PAYLOAD：PROTO_LEN 与块内 **LEN** 数值**一致**（常见 2 字节与 4 字节后两位对齐，见 §3.2）。

### 3.2 块内 `LEN`（`00000001` 后 2 字节大端）

- **含义**：从 **`00000001` 第一个字节起（含）到该逻辑块末尾** 的总字节数（见长度分析文档 §2.1）。  
- **高熵区变长或变短**（B 类非等长替换时）：必须同时改  
  - **修改点 1**：`[4:8]` PROTO_LEN  
  - **修改点 2**：`00000001` 后的 2B LEN  
  若单块包中二者原本相等，替换后仍应**相等**（`3366协议01_0A_00_09长度字段分析.md` §四「核心替换规则」）。

### 3.3 多块 PAYLOAD

若 PROTO_LEN **大于** 某一块的 LEN（载荷内还有其它 TLV/结构），只改**当前块**的 LEN 与块内尾部，并**重算** PROTO_LEN 为「所有子结构总长度」；避免只改一处导致解析错位。

### 3.4 其它可能引用总长度的位置

若存在 **嵌套长度**（如某 TLV 内再带长度），需按实际样本解析链式更新；**仅改 PROTO_LEN/LEN 而不改内层长度** 会导致服务端解析失败。建议对「干净样本」与「脏样本」做 hex diff，列出所有长度字段再实现自动化。

---

## 四、计数器与序号（避免被判定重放）

`3366分析报告.md` §八。

| 字段 | 位置（约） | 要求 |
|------|------------|------|
| 外层 MSG_SEQ | 明文 `[0:4]` 与 33 66 帧头 Seq 小端 | 与当前连接发送节奏一致；重放/替换时勿无故回退。 |
| CTR_PLAIN | byte[29] 一带 | 上行单调递增（与下行合并计数时允许跳变，但上行勿回退）。 |
| CTR_ALL | byte[41] 一带 | 连接级全量计数，单调递增。 |

替换「脏数据」为「干净内容」时，若未改包顺序，**通常保持原 CTR**；若从池取整包替换，必须用**当前连接上一条已发送包的上界 +1** 覆盖，避免回退。

---

## 五、密钥与解密（前置条件）

`3366_密钥与解密说明.md` 全文。

1. **暗区 `0000094E` + `aes_cbc_4013`**：需从**首条下行 10 02** 取 **16B Key**，IV 固定 `00 01 02 … 0F`。  
2. 解密 40 13：`cipher = frame[25 : 25 + enc_len]`，再 TAES 剥填充得 `plain`。  
3. 若解密失败：检查 `3366_key_offset` / `3366_iv_offset`、Key 是否写入 `_3366_aes[client_ip]`。

无有效 Key 则无法对明文做替换或校验长度字段。

---

## 六、推荐实施流程（单帧）

适用于代理内「解密 → 改明文 → TAES 再加密 → 写回」路径（与 `dl_intercept.py` 中 `_intercept_one_3366_frame` 思路一致）。

1. **解析帧头**：`parse_3366_header`，确认 `40 13`。  
2. **解密**：`try_decrypt_4013_frame(frame, key, iv)` → `plain`。  
3. **分支处理**：  
   - **A 类**：在 `plain` 中定位设备字符串 TLV（搜索 `/usr/lib/`、`TweakInject`、`auto_defence_start` 等），向前找到 `len` 字节，向后找到 `;model:` 的位置，**只替换来源标识符部分**（`;model:` 之前），保留 `;model:xxx;ver:xxx;...` 不动；等长替换无需改长度字段，变长替换须**重算** `len` 字节、PROTO_LEN 及块内 LEN（见 §三）。  
   - **B 类**：定位 `01 0A 00 XX`，按 `3366分析报告.md` §10.2–10.3 跳过 8+1+1 字节后替换高熵区；若等长则**不必**改 PROTO_LEN/LEN。  
4. **修补 CTR**：按需更新 byte[29]、byte[41]（见 §四）。  
5. **TAES 加密**：`_taes_encrypt(new_plain, key, iv)` → `new_cipher`。  
6. **写回**：`new_enc_len = len(new_cipher)`，写 `frame[19:21]`，替换 `frame[25:]`，**截断或延长帧**至 `25 + new_enc_len`。  
7. **转发**：整帧发送，避免在 TCP 半包上截断。

---

## 七、风险与边界

| 风险 | 说明 |
|------|------|
| **A 类字符串全量置零** | 若将整个设备字符串（含 `;model:xxx;ver:xxx;...`）全部清零，服务端找不到 `model:`/`ver:`/`inc_id:`/`obf_id:` 键 → **解析失败，可能断连**。只能置零/替换 `;model:` 之前的来源标识符部分。 |
| 服务端校验内层密文 | B 类高熵区来自反作弊模块加密，**同长度干净池替换**相对安全；明文乱改可能触发校验失败。 |
| 长度不一致 | PROTO_LEN / LEN / TLV 内长 **任一处** 错误即可能导致断连或异常日志。 |
| 整包丢弃 | 不更新 CTR 时看似省事，但易因缺包导致状态异常；**不如** A 类改字符串或 B 类同长替换。 |
| 合规 | 篡改反作弊上报可能违反游戏 ToS；本文仅作协议与工程说明。 |

---

## 八、文档与代码索引

| 主题 | 文档 | 代码（参考） |
|------|------|--------------|
| Key / 产品 | `3366_密钥与解密说明.md` | `protocol_3366.py` `decrypt_plain_for_strategy`、`try_extract_key_iv_from_first_downlink` |
| PROTO_LEN / LEN | `3366协议01_0A_00_09长度字段分析.md` | 替换逻辑需自实现或扩展 `process_dl_intercept` |
| TAES / enc_len / CTR | `3366分析报告.md` | `protocol_3366.py` `_taes_encrypt`、`_extract_4013_cipher_by_spec` |
| 下行拦截帧级 | `3366分析报告.md` §十一 | `dl_intercept.py` `_intercept_one_3366_frame` |

---

## 九、小结

- **A 类设备字符串**：字段格式为 `[len][来源标识符];model:xxx;ver:xxx;inc_id:xxx;obf_id:xxx`。  
  - **只能替换来源标识符**（`;model:` 之前），不能全量清零，否则服务端解析失败。  
  - 等长替换（置零或伪造 `cs:xxx` 格式）：无需改任何长度字段，最简单。  
  - 变长替换：**必须**同步更新 `len` 字节、PROTO_LEN、块内 LEN（见 §三）。  
- **密文长度变化**：由 **TAES 填充后的明文长度** 决定，**必须**重算 `enc_len` 与帧总长 `25 + enc_len`。  
- **明文变短/变长（A 类变长）**：**必须**同步 **PROTO_LEN**，若存在 `00000001` 块则同步 **2B LEN**；多块时重算总 PROTO_LEN。  
- **B 类同长度替换**：**优先**保持 `plain` 总长不变，**enc_len 与长度字段**通常最稳。  
- **CTR/SEQ**：避免回退，与 `3366分析报告.md` §八 一致。
