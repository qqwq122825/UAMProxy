# 01 完整模板游标循环重放指南

## 1. 当前简单模式

当前阶段只处理 **01 外层加密包的完整模板重放**，不解密、不改写明文，也不再以
实时脏包为主体拼接加密记录。

核心流程：

```text
收到 42B 加入包
→ 清空当前会话已有 01 池
→ 录制完整干净 01 物理帧组
→ 从 0A 00 23 识别游戏 ID
→ 按游戏 ID 隔离录制池
→ 重放时按游标循环取一份完整模板
→ 写入实时外层会话字段和实时报告序号
→ 重算完整逻辑 payload CRC32
→ 校验长度、分片、CRC、游戏 ID
→ 校验通过后发送
```

模板的高熵数据、物理长度和分片结构来自录制池。`01 0A 00 23` 后的 BE32
报告序号/封包次数来自实时包；该字段属于外层 CRC 覆盖的逻辑 payload，所以覆盖后
必须重算 CRC32，并把新 CRC 写入所有输出分片。

## 2. 录制池规则

每个 01 池项至少保存：

```text
template_frames       完整物理帧列表，按分片号排序
account_id            从 0A 00 23 解析出的游戏 ID
report_index          01 0A 00 23 后的报告序号
crc                   模板逻辑 payload 的 CRC32
routing               首片 0x2F 传输标签
encrypted_record      01 0A 00 09 的完整加密记录元数据
```

隔离和替换规则：

1. 游戏 ID 是选择录制池的主键，不同游戏 ID 的模板不混用。
2. 新录制会话识别到 42 字节加入包时，原地清空当前会话旧 01 池。
3. 新录制确认到与历史相同的游戏 ID 时，新 01 池替换旧 01 池，不做尾部追加。
4. 同一轮录制内的新模板按到达顺序保存，重放游标按此顺序循环。
5. 3366 池与 01 池分开；清空 01 池时保留当前会话已录制的 3366 数据。

导入导出使用 v5 JSON，`template_frames` 以十六进制数组保存。旧格式若只有高熵
记录而没有完整物理帧，不参与当前完整模板重放。

## 3. 游戏 ID 匹配

重放连接先通过实时 `0A 00 23` 识别游戏 ID，再选择同 ID 的录制池：

```text
live_game_id == selected_pool.game_id
template.account_id == selected_pool.game_id
final_output.account_id == selected_pool.game_id
```

首次识别后，连接保存 `expected_game_id`。后续实时 01 包即使没有再次携带账号字段，
仍使用连接上已确认的 ID 校验模板。实时包若显式带出另一个 ID，本包直接丢弃。

## 4. 游标循环

假设同一游戏 ID 的录制池为：

```text
T0, T1, T2, ... T79
```

重放顺序为：

```text
T0 → T1 → ... → T79 → T0 → T1 → ...
```

游标属于当前重放连接。连接开始或重新匹配游戏 ID 时归零。取模板时会跳过：

- 缺少 `template_frames` 的旧池项；
- 游戏 ID 不一致的池项；
- 长度、分片或 CRC 自检失败的池项。

整池没有可用模板时，本次实时包丢弃并记录原因。

## 5. 字段来源

输出以完整干净模板为底稿，只覆盖实时连接必须继承的动态字段。

颜色来源图规则：**蓝色取实时包，黄色重算，绿色保留干净模板。**

### 5.1 首片

| 字段 | 偏移 | 来源 |
|---|---:|---|
| 完整物理帧及高熵数据 | 全帧 | 干净模板 |
| 会话/帧序号 | `0x06..0x09` | 实时包首片（蓝色） |
| 固定产品字段 | `0x0A..0x11` | 干净模板（绿色） |
| 会话字段及包组次数 | `0x12..0x25` | 实时包首片（蓝色） |
| 分片总数 | `0x26..0x27` | 干净模板 |
| 完整 payload CRC32 | `0x28..0x2B` | 写入实时报告序号后重算（黄色） |
| 首片标志、payload type | `0x2C..0x2E` | 干净模板 |
| 传输标签 | `0x2F` | 实时包首片（蓝色） |
| 分片编号、数据长度 | `0x31..0x36` | 干净模板 |
| `01 0A 00 23` 后 BE32 报告序号 | payload 内 | 实时包（蓝色） |
| payload 其余内容 | `0x37..` | 干净模板 |

### 5.2 后续分片

若干净模板有多个分片：

1. 保留模板的分片数量、分片编号、数据长度和分片数据。
2. 所有输出分片使用实时包的同一个包组/次数。
3. 帧序号从实时首片序号开始连续递增，溢出后按 BE16 回绕。
4. 后续分片的 `0x2F..0x32` 是数据长度，不能当传输标签覆盖。
5. 所有分片写入修改后完整 payload 的同一个新 CRC32。

## 6. 最终发送校验

每个准备发送的完整 01 帧组都执行轻量校验：

```text
每个物理帧声明长度 == 实际长度
分片总数、分片编号连续且唯一
首片存在且编号为 1
所有分片的包组、分片总数、CRC 一致
每片声明数据长度 == 实际分片数据长度
重组 payload 后 crc32(payload) == 输出头的新 CRC
输出中解析出的游戏 ID == expected_game_id
```

其中 CRC 对比对象是输出头中写入的**新 CRC**；录制模板 CRC 只保留作审计比较。

任何一项失败，本帧组不发送，并写入 `[01模板丢弃]` 日志。

每个重放连接的前 10 个成功输出额外记录：

```text
池游标位置
游戏 ID
报告序号
输入与输出总长度
输入与输出 payload 长度
输出分片数
CRC
长度/分片/CRC/游戏 ID 校验结果
```

启用详细 01 日志时，前 10 个成功输出再附带实时输入和最终模板包的完整十六进制。
第 11 个以后仍执行相同校验，只减少日志量。

## 7. 双样本结果

以：

```text
01 原始数据包上传.md   = 干净模板池
01 原始数据包上传2.md  = 实时输入
```

为例，两者游戏 ID 均为 `933579391`。输出依次取干净池报告 4、5、6：

```text
高熵数据、帧长、分片：使用干净模板
报告序号：使用实时包
CRC：写入实时报告序号后重算
实时帧序号：25546、25547、25548
实时包组/次数：6、7、8
实时传输标签：0x2B
实时会话头：继承样本2
```

最终三个 CRC 分别保持：

```text
7279A388
E49C0CA4
2AF9A574
```

本例两组样本的实时报告序号与对应模板同为 4、5、6，因此写入后的 payload 未发生
字节变化，CRC 数值恰好仍与模板相同。如果实时报告序号不同，输出 CRC 会随之变化。

## 8. 后续明文改写扩展

未来增加明文字段改写时，在“选中模板”与“最终校验”之间增加独立步骤：

```text
选中完整模板
→ 解密模板记录
→ 只改目标明文字段
→ 重算明文 CRC 并重新加密
→ 重建相关长度和外层 CRC
→ 覆盖实时会话字段
→ 执行同一套最终发送校验
```

当前简单模式不进入这条分支，确保完整模板路径保持短、稳定、可审计。
